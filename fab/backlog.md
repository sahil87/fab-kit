## Open

- [ ] [ngaw] 2026-02-23: Quality gate - how to decide which PR has had deep thought vs just surface level?
- [ ] [v34t] 2026-02-23: A timeline or user journey mermaid diagram showing which commands are typed in the main repo vs the worktree
- [ ] [q0lw] 2026-03-11: If fab binary or wt or idea binary not found, stop. Add to preamble?
- [ ] [ub2y] 2026-04-02: Make hooks work directly using the fab system command - remove fab/.kit folder dependency from everywhere
- [x] [1old] 2026-04-04: [DONE: shipped via parent dn2c, PR #397 merged 2026-06-12 — absorbed, no standalone PR.] fab init should check for git repo before downloading release — currently downloads first, creates config, then fails at sync's git check, leaving stale artifacts. ABSORBED into [dn2c] (binary-review B3) — pick up there, do not work this entry standalone.
- [ ] [24wt] 2026-06-01: Check if there's a more effecient scording scheme than SRAD right now
- [x] [x8c9] 2026-06-02: [DONE: shipped via parent ye8r, PR #402 merged 2026-06-12 — absorbed, no standalone PR.] Consolidate gating/scoring data into one source of truth. Per-change-type values are currently spread across multiple places — gateThresholds (flat 3.0) and expectedMin (feat:7/refactor:6/fix:5, default 3) in src/go/fab/internal/score/score.go, mirrored by hand in docs/specs/change-types.md (gate + expected_min tables). No automated check ties code and doc together, so they silently drift (the v1.10.0 spec-merge had to reconcile a pre-existing code/doc drift). Reduce to a single canonical definition (e.g. generate the doc table from the Go maps, or a shared data file) so changing a threshold is a one-place edit. ABSORBED into [ye8r] (binary-review B4) — pick up there, do not work this entry standalone.
- [x] [dkn3] 2026-06-12: Expose the stage state axis in 'fab pane map --json'. DisplayStage (src/go/fab/internal/status/status.go:463-498) computes (stage, state) but both callers discard the state half — panemap.go:324 and internal/pane/pane.go:152 — so downstream consumers (run-kit sidebar) cannot distinguish an actively-worked stage from a parked finished change: a fully-shipped change displays 'review-pr' indefinitely until archived, identical to review-pr actively running. Add display_state (active/ready/done/failed/pending/skipped) as a nullable field on pane map JSON rows (additive shape change; run-kit consumes it via app/backend/internal/sessions/sessions.go paneMapEntry and would update its docs/specs/api.md separately). Unlocks honest per-row attention states in run-kit (failed/ready = needs human, done+parked = quiet row) instead of heuristics over agentState/.fab-runtime.yaml idle_since. ABSORBED into [pw3k] (binary-review B5), then SHIPPED STANDALONE as 260612-dkn3-pane-map-display-state (PR #394, merged 2026-06-12) including the DisplayStage `failed` tier pw3k had deferred; pw3k rebased its duplicate display_state work away.
- [x] [k4ge] 2026-06-12: Skills-audit batch 1/5 — CLI exit-code contract conformance (Go + tests + doc rows). SHIP FIRST — one item bypasses the pipeline's only safety gate. GOAL: make the binary honor the contracts the skills document, then fix the doc rows that were wrong about the binary. ACTIONS (report §2 Theme 1): fab score --check-gate must exit non-zero on gate fail — cmd/fab/score.go:25-32 prints the gate YAML and returns nil unconditionally, while _preamble.md:230 / _cli-fab.md:91 / _pipeline.md Pre-flight 3 all document non-zero-on-fail, making the ff/fff intake gate undetectable via the documented contract (empirically reproduced: gate fail on stdout, exit 0). lookupTransition (internal/status/status.go:21-61) must reject transition targets not in AllowedStates — today (advance ship|review-pr) writes schema-forbidden (ready) and (skip intake) writes (skipped), permanently bricking preflight for that change ("State 'ready' not allowed for stage ship"; reproduced). Fix fab change switch Next: routing off-by-one at post-review stages (internal/change/change.go:217-223 prints /fab-archive where /git-pr-review is correct; diverges from fab-status for the same state). Archive exit semantics: ExactArgs(1) on fab change archive (no-arg currently exits 0 with help text); document or fix archive partial failure (YAML + non-zero) and the unreachable re-archive soft skip (_cli-fab.md:42-46, fab-archive.md:110 — genuinely archived changes exit 1 "No change matches"); fab batch archive exits 1 on empty sets with failed==0; restore --switch swallows activation failure (internal/archive/archive.go:171-178) — surface (pointer: failed). Fix the stage_metrics cascade so review Iterations survives the fail+reset choreography — SPEC-_pipeline.md:20's PR-meta rationale is currently false (always "1 cycle"); preserve the counter or derive from .history.jsonl. fab hook sync violates the "All hook subcommands exit 0" claim (_cli-fab.md:168). THEN the doc pass: correct rows that remain intentionally exit-0, and fix _preamble.md:232's invalid canonical form (fab change resolve --folder) — unknown flag, only top-level fab resolve registers the query flags (reproduced). CONSTRAINTS: constitution — Go changes need tests + same-PR _cli-fab.md updates; SPEC mirror per touched skill. REPORT: docs/specs/findings/skills-review-2026-06-12.md §2 Theme 1 (+ Theme 4 for the metrics item); line numbers vs commit 1431a9c3.
- [x] [w7dp] 2026-06-12: Skills-audit batch 2/5 — orchestrator dispatch targets + review-pr failure recovery. DEPENDS: wave 2 — after k4ge merges (skill text references the fixed exit-code contract) AND after g8st merges (collides with it on git-pr.md Step 0/guard block, git-pr-review.md failure paths, fab-operator.md §6). GOAL: every inter-skill hand-off names a verified target; no dead-end states. ACTIONS (report §2 Themes 2+4): give /git-pr and /git-pr-review an explicit change argument + a branch-matches-change guard — fab-fff.md:39-61 Steps 4-5 pass (change: {id}) but both skills self-resolve only the ACTIVE change, so under the advertised <change-name> override Steps 1-3 work the override change while ship/review-pr mutate the active change's status and push whatever branch is checked out (must-fix). Replace the unexecutable (/fab-clarify intake) recovery at all 5 call sites (_pipeline.md:106 + 4 in fab-continue.md) — (intake) parses as a change-name substring, clarify is stage-guard-blocked post-intake, and the promised requirements regeneration never happens. fab-operator: fix the autopilot double-dispatch (spawn step 5 at :391 embeds '<command>' while Gate/Dispatch follow at :489-492 — a #393 f049 regression); replace (/fab-switch <change> && /fab-proceed) at :463 with a single parseable command (/fab-fff <change> — && chaining has no slash-command semantics); reconcile queue-chaining semantics (:475/:481/:511 — strict queue-previous vs nearest-same-repo-predecessor produce different worktrees; pick one, fix rule or examples). Add the missing review-pr/failed dispatch row to fab-continue Step 1 (3 lenses converged: preflight at that state currently matches "all done" and reports a FAILED pr-review as complete; route to re-execute /git-pr-review — its Step 0 start already handles failed→active). Make Reset Flow idempotent when the target stage is already active (fab-continue.md:193, Constitution III); add (skipped) to the reset From-set (:85); fix the intake.md-missing pointer loop (:204). Align every state enumeration to status.go AllowedStates: fab-continue.md:57-58 unreachable (ready) rows, fab-switch.md:95 missing ready/skipped qualifiers, fab-status.md:46 missing skipped glyph. Define the underspecified dispatch inputs: fab-proceed.md:163 fab-new subagent behavior when SRAD must ask the user (promptless context, no [AUTO-MODE]); _review.md:53 inward sub-agent change_type skip condition has no defined input; fab-new.md:50 backlog-ID collision pre-check is substring-based (false positive silently skips creation — anchor on the [id] token). Drop the stale /git-branch chain after fab-new (fab-proceed.md:89/91 — no-op since #322's inline branch creation) + fix _cli-external.md:59-63 worktree-branch claim. Reconcile {driver} claims (fab-ff.md:32 / fab-fff.md:32 "passed to every fab status event command" vs _pipeline's deliberate driver-less fail/recovery commands) and _pipeline.md:87-92's two competing rework routes for "requirements mismatch". CONSTRAINTS: SPEC mirror per touched skill; src/kit is canonical. REPORT: docs/specs/findings/skills-review-2026-06-12.md §2 Themes 2+4.
- [x] [g8st] 2026-06-12: Skills-audit batch 3/5 — autonomous git skill state hardening. PARALLEL: wave 1 — safe alongside k4ge and c5tr (same-file-different-section touches only; coordinate the fab-archive exit-semantics seam with k4ge, which owns the Go/doc side of that contract). COLLIDES with w7dp on git-pr.md Step 0/guard block, git-pr-review.md failure paths, and fab-operator.md §6 — do NOT run concurrently with w7dp; w7dp branches after this merges. GOAL: the no-questions skills (git-pr, git-pr-review, git-branch, fab-new step 11, fab-archive, operator git ops) stop assuming a clean/attached/main-defaulted/local-only world. ACTIONS (report §2 Theme 3): detached-HEAD STOP before the autonomous commit path — git-pr.md:104-121/156-163 currently passes the branch guard then commits and emits a refspec-less push (must-fix). Split push-failure handling in git-pr-review.md:139-149/180 — "(no partial state)" is false (git reset cannot undo the commit) and the re-run path declares "No changes needed" / posts "Fixed" replies citing an unpushed SHA, permanently stranding fixes (must-fix): keep the commit, document recovery, add an unpushed-commit check to the re-run gate. Scope git add -A (git-pr.md:143-150) — it sweeps every untracked repo file into a pushed commit; stage known paths or guard with a stop-on-unexpected-untracked check. Branch has_pr on PR state (git-pr.md:70-127 — state/number are fetched and never read; a closed/merged PR short-circuits creation): branch OPEN/MERGED/CLOSED explicitly. git-branch.md:53-62/85-90: STOP with a disambiguation list on ambiguous multi-match instead of silently creating a junk standalone branch with a false message; check out remote-only branches with --track origin/<branch> instead of recreating divergent locals. Resolve the actual default branch instead of literal main/master (git-pr.md:106) — also fixes operator cherry-pick/rebase hardcoding origin/main with no fetch step (fab-operator.md:429-445/485/499, autopilot unusable on non-main-default repos). Dirty-tree caveat at change creation: fab-new.md:154-160 / git-branch.md:140 — uncommitted work silently rides into the new change's branch; warn or stash-prompt. fab-archive: archive/restore move tracked files + edit fab/backlog.md with no commit step (the "safe" claim contradicts the dirty tree it leaves); the archive-ok/backlog-mark-failed exit has no recovery path (re-run can never mark the backlog — make backlog marking idempotent on re-run). CONSTRAINTS: these are behavior changes to autonomous paths — flag each in the PR; SPEC mirror per touched skill. REPORT: docs/specs/findings/skills-review-2026-06-12.md §2 Theme 3.
- [x] [c5tr] 2026-06-12: Skills-audit batch 4/5 — scaffold/config truth + SRAD scoring coherence. PARALLEL: wave 1 — safe alongside k4ge and g8st (overlaps are same-file-different-section only: _preamble.md:40 vs k4ge's :230-232, fab-new.md:182-189 vs g8st's :154-160, shared SPEC mirrors; second-to-merge rebases). GOAL: what ships to new projects and what the scoring layer computes both match the 6-stage reality. ACTIONS (report §2 Themes 5+6): SCAFFOLD — src/kit/scaffold/fab/project/config.yaml:31-38 still seeds stage_directives.spec (must-fix): new projects never run the relocating migration (fab init stamps fab_version past it), so the zombie key is permanent; move/cull the directives — the relocated "Mark ambiguities with [NEEDS CLARIFICATION]" apply-directive contradicts src/kit/templates/plan.md:36 (markers are intake-only post-1.10.0); this repo's own fab/project/config.yaml:19-21 carries the same residue, clean it too. Decide wire-or-remove (ask user): stage_directives (scaffold promises it, fab-setup.md:167 edits it, three migrations preserve it, ZERO readers in skills or Go) and stage_hooks (LIVE in config.go:18 + status.go:603-621 — pre blocks start, post runs after save — documented NOWHERE, incl. the failing-post-hook re-run trap where the stage is already done); model_tiers likewise has zero consumers. Retire-or-regenerate src/kit/schemas/workflow.yaml — still defines the 7-stage pipeline with spec (must-fix); nothing consumes it yet user-flow.md:201 calls it source of truth (recommend retire + repoint to the Go state machine). Restore the one-line semver-comparison rule fab-setup's three-way branch needs (#393 f080 dedup deleted it; fab-setup.md:303 vs :294); fix the fab_version create-mode fallback (:95/:153) and Next-Steps drift (:430-436). Scaffold code-review.md:44 names the removed "revise spec" escalation; the "Max cycles: 3" knob is consumed by nothing (_pipeline hard-codes 3) — wire or annotate. _preamble.md:40 always-load descriptor still advertises removed naming + dead model tiers. SRAD — half-open grade bands ≥85/≥60/≥30/else (closed integer bands at _srad.md:30 leave composites like 59.85/84.5 ungraded; boundary worth 0.7 score/row); ONE Critical-Rule number (_srad.md:47 <25 override vs :30 0-39 Low band); re-dimension Worked Example 3 (Certain grade mathematically unreachable, cap 84.75<85, _srad.md:71); fix srad.md:54-118 Assumptions-table contract (Scores "optional", Certain/Unresolved rows omitted — emits tables fab score cannot fully parse, deflating gate inputs) + Example 1 arithmetic (:246-248); fab-clarify: evaluate bulk-confirm BEFORE the Step 1.5 zero-gaps early exit (must-fix; :55 vs :61-73 — a below-gate Confident-only intake currently dead-ends at "artifact looks solid") + S→95 upgrade labels rows Certain below the 85 threshold (:110-118) + audit-trail asymmetry (:122/:153); fab-new.md:182-189 output ordering violates _srad's Assumptions-block-last SHALL; _generation.md:75-78 plan walk never emits the ## Assumptions section it depends on (add the explicit step; reconcile omit-when-zero vs scaffolded templates); _srad.md:51-57 autonomy table covers 4 of the 6 declaring skills. CONSTRAINTS: removals need migrations in src/kit/migrations/; Go changes need tests + _cli-fab updates; SPEC mirrors incl. docs/specs/srad.md. REPORT: docs/specs/findings/skills-review-2026-06-12.md §2 Themes 5+6.
- [x] [d9rs] 2026-06-12: Skills-audit batch 5/5 — docs reality sweep + quick-win bundles. DEPENDS: LAST of the five — mirrors/docs must describe post-batch reality. GOAL: the documentation layer describes the shipped system; burn down the quick wins. ACTIONS (report §2 Themes 7+8, §3): SPEC-HOOKS rewrite as-shipped (3 must-fix: Current Hooks lists two deleted shell scripts; "Proposed Hook Architecture" presents shipped Go behavior as future work; events table rates UserPromptSubmit "No" while that hook is registered; plus superseded fab runtime proposal, dead yq inventory, stale phase list, outdated runtime schema). LEGACY DOCS truth sweep (extends the deferred uliv doc-residue list): assembly-line.md:121 spec/tasks narrative; architecture.md pre-binary .kit/ distribution model + Router Dispatch self-contradiction (consider rewrite vs retire); overview.md 4-stage story omitting /git-pr /git-pr-review /fab-proceed /fab-operator; glossary.md:49/115 auto-clarify fab-ff disclaims; user-flow.md:84/183 "failed is review-only"; templates.md pre-1.10.0 intake template + .status.yaml block missing ready/id/issues/prs; skills.md:583 pre-date-bucketing archive path; operator.md:11 v8→v9. SPEC MIRROR resync (~18 drifted, enumerated in Theme 7c — e.g. SPEC-fab-operator status-only-mode + rejected Decision 2 recorded as current, SPEC-_preamble misquoted opening instruction + dead kit.conf row, SPEC-fab-continue writes a removed "Spec" artifact + claims forbidden fab score use, SPEC-fab-archive #393 f087 regression, SPEC-fab-clarify removed [target-artifact] flow, SPEC-fab-proceed _preamble self-contradiction). MEMORY-INDEX ownership (Theme 8): docs-reorg-memory.md:125-126 Step 5.3 edits a sub-domain index that doesn't exist until Step 5.4 generates it AND 5.4 forbids the edit (must-fix; reuse the stub pattern at docs-hydrate-memory.md:69); define ownership once — description: frontmatter is the single hand-curated field, stub created BEFORE fab memory-index — propagate to hydrate generate-mode (no placement rules today), docs-hydrate-specs no-target-spec branch + phantom skip token, docs-reorg depth off-by-one + dangling-link abort escape, docs-reorg-specs reserved-path exemption for constitution-pinned SPEC mirrors. QUICK-WIN Bundle A stale pointers (§3 has the full list): fab-operator.md:23 §5→§6, :226-252 "7 tracked" vs 8 entries + gmail-deploys schema-invalid source, :192 stale retention clause; fab-proceed.md:109 "(see Output Format)"→Bypass Notes; fab-continue.md:149 dangling "Review Behavior" heading; git-pr-review.md:105 unconsumed node_id + :11/64 --tool header; ~12 more one-liners. Bundle B enumeration completion: _preamble.md:36 exceptions list (+/fab-proceed /fab-help /fab-archive /docs-hydrate-specs /docs-reorg-*), :301 orchestrator list omits /fab-proceed, :355-361 scoring invokers omit /fab-draft; internal-skill-optimize.md:15/21/86 omits _pipeline (stale twice now); _generation.md:3/11-13 fab-continue in both consumer groups; _cli-fab.md:27 index omits migrations-status/memory-index; Next: lines omit /fab-proceed (fab-draft:30/48, fab-new:223, fab-setup:432-436). Root-cause: prefer derivation rules over enumerations ("every _*.md file is a shared partial"; "derive Next: per the Lookup Procedure") so these cannot drift a third time. HYGIENE: archive the four merged-but-unarchived 2606xx change folders. CONSTRAINTS: prose-only, no behavior changes; SPEC mirror per touched skill; src/kit is canonical. REPORT: docs/specs/findings/skills-review-2026-06-12.md §2 Themes 7+8 + §3.
- [x] [mz4q] 2026-06-12: Binary-review batch B1/6 — shared-state concurrency + hook hot path. PARALLEL: wave 1 — runs alongside k4ge, dn2c, pw3k; seam with k4ge on internal/status/status.go (k4ge owns lookupTransition/AllowedStates, this owns the Save/SetAcceptance paths — same-file-different-function, second-to-merge rebases). GOAL: shared mutable state survives concurrent agents (the operator premise) and the per-edit hook path does minimal work. ACTIONS: F01 one flock helper (sibling .lock file, syscall.Flock LOCK_EX — linux+darwin only per the proc build tags) wrapping all four .fab-runtime.yaml mutators (runtime.go:183-345; two racing stop-hooks currently lose an agent entry: A loads {}, B loads {}, A saves {A}, B saves {B}). F03 reuse the helper for .status.yaml load-mutate-save cycles (the artifact-write hook holds a stale snapshot across 4 Saves while fab status start/finish runs in other panes) + add tmp.Sync() before Close in statusfile.Save to match runtime.SaveFile's durability posture. F02 the artifact-write hook fires on EVERY plan.md/intake.md Write/Edit: split mutation from persistence so plan bookkeeping does ONE Save instead of 4 SetAcceptance round-trips (hook.go:281-296), and the intake branch resolves the change folder once instead of 3x (hook.go:204 / score.go:130 / log.go:53) via a score entry point accepting the already-loaded StatusFile. F04 fold GCIfDue into the same load/save as the agent-entry mutation in session hooks. F05 atomic .fab-status.yaml pointer swap (write temp symlink + rename). F08 validate the symlink target exists before trusting it. F06 stop reporting every .status.yaml read error as 'status file not found' (classify open vs parse vs permission). F07 error/warn when a .status.yaml write targets an absent key or progress stage instead of silently dropping. CONSTRAINTS: constitution — Go changes need tests; _cli-fab.md only where documented messages change; src/kit canonical. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B1 F01-F08 (line numbers vs commit 1431a9c3).
- [x] [hv7t] 2026-06-12: Binary-review batch B2/6 — scanner-truncation sweep + score truth-telling. DEPENDS: wave 2 — branch after k4ge merges (collides on cmd/fab/score.go and the _cli-fab.md fab-score rows; k4ge defines the gate exit-code scheme this batch's error surfacing must conform to). GOAL: the binary either reads the whole file or says it didn't — no silent truncation, no lying success. ACTIONS: kill the unchecked-bufio.Scanner class — 12 of 13 production sites never check scanner.Err(), none call scanner.Buffer (F13 lists every site); standardize on a shared read-lines helper (os.ReadFile + strings.Split; TrimSuffix each line of \r to stay CRLF-behavior-preserving — verifier caveat) and migrate all sites. Highest stakes: F09 countGrades (score.go:228-240) truncation on a >64KB intake.md line can flip the single intake gate from hard-fail 0.0 to PASS by dropping unresolved rows — the gate that authorizes ff/fff autonomy. F10 archive removeFromIndex (archive.go:432-463) rewrites archive index.md from a truncated scan — destructive loss of every entry after the abort point; use the os.ReadFile+Split pattern backlog.MarkDone already does right. F12 backlog ParsePending/ExtractContent silently drop items + swallow os.Open errors (batch-new --list/--all shrink with zero indication). F11 fab score stops lying: surface .status.yaml load failures (currently skips persistence entirely and silently defaults change_type to feat), stop discarding SetConfidence/ConfidenceLog errors with _ =, propagate countGrades read errors so read-failure is distinguishable from an empty Assumptions table. F14 replace batch_archive's regex-scan of .status.yaml with the statusfile package. F15 make archive index updates atomic + check write errors. CONSTRAINTS: constitution — tests per Go change; no CLI signature changes expected. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B2 F09-F15 (vs 1431a9c3).
- [x] [dn2c] 2026-06-12: Binary-review batch B3/6 — fab-kit download & lifecycle hardening. PARALLEL: wave 1 — separate Go module (src/go/fab-kit), no file overlap with k4ge/mz4q/pw3k; seam with tb6f on .github/workflows/release.yml (this adds checksum publishing, tb6f adds the test step — tb6f goes last and rebases). ABSORBS [1old]. GOAL: the shim's auto-download can't hang, can't exec a partial binary, can't run unverified bytes; lifecycle commands tell the truth in exit codes. ACTIONS: F16 dedicated *http.Client with Timeout (+ ResponseHeaderTimeout) for Download and LatestVersion — http.Get/http.DefaultClient today (download.go:30,64); every uncached `fab <cmd>` (even `fab --help` via printHelp→EnsureCached) can wedge indefinitely on a black-holed connection, multiplied by operator pane fan-out. F17 extract to versions/<v>.tmp-<pid> + atomic os.Rename under a version-keyed flock — fab-go's exec bit is set at OpenFile time BEFORE io.Copy finishes, so a concurrent process can syscall.Exec a truncated binary (readiness check is mode&0111 only). F20 publish SHA256SUMS for the kit-* archives in release.yml (today only brew-* archives get hashes, computed inside the formula generator) and verify the digest before extract/exec. F18 upgrade-repo: propagate Sync failure as non-zero and roll back the fab_version stamp — today it stamps BEFORE sync, converts failure to a WARNING, prints 'Updated: x -> y', exits 0, and the re-run short-circuits on the already-stamped version (unrecoverable via the same command). F19 versionGuard: Update() returns nil after merely printing 'not installed via Homebrew', silently defeating the guard for go-install/manual/CI installs — return ErrNotBrewInstalled and fail the guard with actionable instructions; after an in-guard update, re-exec or fail with 're-run fab sync' instead of continuing on the old binary. F21 check write errors in deploySkills/scaffolding (success counts include failed writes). F22 pass the real fab-kit binary version to Sync from Init/Upgrade — both currently bypass versionGuard by passing the kit version as systemVersion. +[1old] fab init checks for a git repo BEFORE downloading the release (today: download, write config, fail at sync's git check, stale artifacts left behind). CONSTRAINTS: constitution — tests for the changed contracts (the broader fab-kit test suite is tb6f's F45); _cli-fab.md rows for init/update/upgrade-repo exit semantics. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B3 F16-F22 (vs 1431a9c3).
- [x] [ye8r] 2026-06-12: Binary-review batch B4/6 — CLI single-sourcing + doc conformance. DEPENDS: wave 2 — branch after k4ge merges (collides on _cli-fab.md and the resolve doc rows; k4ge owns the doc-side fix of the invalid `fab change resolve --folder` canonical form, this batch owns the binary side). Coordinate with d9rs on the _cli-fab.md index line (second-to-merge rebases). ABSORBS [x8c9]. GOAL: every hand-maintained duplicate of CLI-surface data gets one source of truth + a drift test; documented surface == actual surface. ACTIONS: F23 collapse the 5x-duplicated workspace-command allowlist (shim fabKitArgs + printHelp's hardcoded names AND Shorts, fab-kit's fabKitCommands, two tautological tests that re-declare the same 6 strings) into one shared LifecycleCommands table in the fab-kit module's internal package; derive shim routing + help from the table (do NOT exec fab-kit --help for help text — verifier caution: keeps no-binary help working); fix _cli-fab.md:17 (omits migrations-status while line 234 asserts it's in the allowlist — drift already shipped via 260610-9733, a constitution-MUST violation); add a changetypes_doc_test.go-style contract test + a collision test against fab help-dump output (NB the hidden command is help-dump, not helpdump). F25 fab resolve: minimum cmd.MarkFlagsMutuallyExclusive over the 5 output bools (panemap.go:29 precedent — today '--status --folder' silently prints folder via a priority chain, and --id is registered but NEVER read); better: single -o/--output id|folder|dir|status|pane with the bools as hidden deprecated aliases. F27 dedupe `change resolve` (literal passthrough of top-level resolve) and resolve --pane's forked pane logic missing --server. F29 batch switch: stop self-exec'ing `fab` through PATH for resolution (version-skew under the pinned-shim model + spawn cost) and reconcile the batch no-arg defaults divergence. F24 document `fab pane window-name` (incl. its bespoke exit codes 2/3 and --json), `fab shell-init`, and `fab change list --show-stats` in _cli-fab.md (all absent today — constitution violation); consider a CI drift check diffing help-dump against _cli-fab.md. F26 consolidate the six ad-hoc fab/project/config.yaml parsers behind internal/config. F28 let `fab log command` own its best-effort contract (always exit 0, warn on stderr) so skills can drop the 2>/dev/null||true boilerplate — coordinate the _preamble.md:231 wording. F30 unify RunE exit-code semantics/error formatting, conforming to k4ge's scheme. +[x8c9] single-source gateThresholds/expectedMin (internal/score/score.go) vs docs/specs/change-types.md — generate the doc table from the Go maps or add a drift test (same pattern as F23; SEAM with hv7t on score.go — data maps vs scan logic, different sections, coordinate). CONSTRAINTS: constitution — every CLI signature/flag change updates _cli-fab.md same-PR; update skills that call changed commands + their SPEC mirrors. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B4 F23-F30 (vs 1431a9c3).
- [x] [pw3k] 2026-06-12: Binary-review batch B5/6 — operator/pane/tmux surface: perf + error surfacing. PARALLEL: wave 1 — alongside k4ge, mz4q, dn2c; touches panemap/pane_*/batch_new/operator/memoryindex only (mz4q owns hook.go + runtime.go, so the GC-fold seam lives there). ABSORBS [dkn3]. GOAL: the operator-facing surface is fast and honest about failures. ACTIONS: F31 batch new discards the tmux new-window error entirely (.Run() with no error variable, batch_new.go:115) — the worktree was already created, the item already printed as launched, and the command exits 0; a blind retry then breaks on the existing worktree (anti-idempotent, constitution III): capture the error, warn with item + orphaned worktree path, track a failure count, non-zero exit on any failure; wrap wt-create stderr too. F38 return errors from RunE instead of calling os.Exit(1) in batch new + operator. F35 include captured stderr in tmux/git subprocess error wrapping instead of bare 'exit status 1'. F33 operator singleton check: exact window-name match (prefix match today). F32 pane map: dedupe the per-pane `git rev-parse` and collapse the N+1 tmux calls into one list-panes -a with format fields. F34 memory-index: one batched `git log` pass instead of per-file `git log -1` spawns. F36 drop the server-wide `tmux list-panes -a` pre-validation in capture/send (verifier confidence MEDIUM — re-verify error-path equivalence before removing). F37 darwin pane process: fetch args in the single `ps -ax` call instead of one ps spawn per node. +[dkn3] expose display_state (nullable: active/ready/done/failed/pending/skipped) on pane map --json rows — DisplayStage already computes (stage,state) but both callers discard the state half (panemap.go:324, internal/pane/pane.go:152); additive shape change; run-kit consumes it separately and updates its own docs/specs/api.md. CONSTRAINTS: pane map --json change is additive-only; _cli-fab.md + fab-operator.md rows for changed output; SPEC mirrors per touched skill. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B5 F31-F38 (vs 1431a9c3).
- [x] [tb6f] 2026-06-12: Binary-review batch B6/6 — tests, CI & toolchain. DEPENDS: wave 3, LAST of the six — after k4ge (state-machine tests must target the FIXED transition semantics incl. AllowedStates hardening), mz4q (locking changes Save behavior), dn2c (release.yml seam + new lifecycle exit contracts to test), ye8r (flag-API changes land before the cobra bump). GOAL: the state layer and destructive mutators are tested, releases can't ship untested, the toolchain is in support. ACTIONS: F39 table-driven exhaustive test over lookupTransition (stage × event × from-state, incl. the review/review-pr failed→active override) + Reset/Skip cascade tests — Advance/Reset/Skip/Fail/SetChangeType/SetConfidence/AddIssue/AddPR/ProgressMap/Validate are ALL at 0.0%, package 37.9% coverage vs 78-100% elsewhere; the loadFixture helper already round-trips a .status.yaml in t.TempDir. F45 test fab-kit's destructive workspace mutators (Sync/Init/Upgrade/deploySkills/cleanLegacyAgents all at 0%). F40 release.yml runs ZERO tests before shipping binaries on a tag push (the if-guard only restricts workflow_dispatch) — add a test step or a reusable job shared with ci.yml, and replace the hardcoded go-version '1.22' with go-version-file (ci.yml:36-38's 'single source of truth' comment is false today). F42 compile darwin in CI (GOOS=darwin go build ./... — proc_darwin.go and the darwin pane-process path are only ever built at release time). F41 toolchain: go 1.22 is out of security support, cobra is two majors behind, yaml.v3 is archived — bump go + cobra; for yaml evaluate goccy/go-yaml vs staying pinned, with golden round-trip tests on .status.yaml FIRST (byte-stable output is a documented property of fab memory-index). F46 cover the cobra wiring RunE bodies (change/log/archive/batch/hook subcommands at 0%). F44 split fab-kit/internal/sync.go (780 lines, six unrelated concerns). F47 remove dead exported funcs + vestigial code INCLUDING src/benchmark/statusman-go, EXCLUDING stage_hooks/model_tiers — c5tr owns that wire-or-remove decision, coordinate. F43 optional/stretch (effort large): move domain logic out of cmd/fab package main into internal packages — take only the pieces that fall out naturally while testing. CONSTRAINTS: constitution — Test Integrity (tests conform to spec, never the reverse); yaml swap must preserve byte-stable index/status output; src/kit canonical. REPORT: docs/specs/findings/binary-review-2026-06-12.md §B6 F39-F47 (vs 1431a9c3).
- [x] [xiqw] 2026-06-13: [DONE: all 3 children merged — fgxx #411, 3xaj #412, m3d4 #413 (2026-06-13).] fab skill refactor (3 coordinated changes, see docs/findings/): EXEC ORDER — [B] 260613-3xaj-extract-intake-helper (independent, no Go, lowest risk) and [A] 260613-fgxx-collapse-post-intake-dual-mode (needs Go history-shape spike) first; then [C] 260613-m3d4-uniform-stage-model-tier LAST (gated on A — closes its Gap 1a). All 3 below 3.0 gate by design — /fab-clarify each before /fab-fff.
- [x] [jznd] 2026-06-15: Binary truth-telling, error-surfacing & inference correctness (fab Go) — GROUP A of 2 (recurring-lessons fixes 1,2,3,6). All anchors verified 2026-06-15 vs current src/go/fab. GOAL: the fab binary computes state honestly, surfaces write errors, and infers change_type correctly. ACTIONS: (a) change_type inference [fix 2 sub-bugs in internal/hooklib/artifact.go: the 'fix' keyword pattern at L96 uses a word-boundary regex that matches 'fix' INSIDE compounds like 'must-fix'/'hot-fix' because the hyphen counts as a word boundary — tighten the pattern so hyphenated compounds don't trip it; AND add an infer-once / respect-explicit-set guard — today hook.go:283 artifactBookkeeping() re-infers on EVERY intake edit and status.go:324 ApplyChangeType() unconditionally overwrites, no guard]. (b) acceptance truth [derive acceptance_completed from plan.md '## Acceptance' checkboxes on read in internal/status + score/preflight readers instead of trusting the hook-maintained counter — closes the sed-bypasses-hooks class AND the existing review-acceptance-checkbox-gap]. (c) F21-residue [scaffold.go:273 and :294 swallow os.WriteFile errors in lineEnsureMerge while the scaffoldDirectories comment falsely says 'write failures are propagated' — surface them + fix the lying comment; skills.go is already clean]. (d) resolve sentinels [internal/resolve has NO typed errors; add ErrNotFound/ErrAmbiguous so archive.go:66-76 + batch_archive.go soft-skip stops conflating not-found with ambiguous]. (e) prmeta clampNonNeg [prmeta.go:255-260 clamp used at :231-233 forces impl net to +0 on test-heavy PRs, hiding real negative net — report honestly or annotate; check binary-review Refuted section before changing]. CONSTRAINTS: Go change MUST add test updates + update src/kit/skills/_cli-fab.md per constitution. SEQUENCING: merge A BEFORE B (if A ships the mirror-lint gate it would otherwise enforce on B mid-flight). FALLBACK: this is 5 distinct Go fixes — if intake scores low or review balloons, peel the mirror-lint (recurring-lessons #1, Group B-lint) into its own change and ship it LAST. SRAD: likely below 3.0 — /fab-clarify before /fab-fff.
- [x] [qg64] 2026-06-15: Pipeline choreography prose fixes (skills only, no Go) — GROUP B of 2 (recurring-lessons fixes 4,5). [SHIPPED: PR #417, merged 2026-06-15.] GOAL: fix two pipeline-behavior defects that live in skill prose, not the binary. ACTIONS: (a) stage_metrics cycle-count [PR-meta under-reports review rework as '1 cycle' when 3+ cycles ran; CONFIRMED the bug is orchestrator/skill choreography, NOT internal/status — a 3-cycle fail->reset->re-finish regression test PASSES against the unchanged state machine. Fix the fail->reset->re-finish sequence in the orchestrator skills (_pipeline.md / fab-ff / fab-fff) so the iteration counter isn't reset to 1; do NOT touch internal/status]. (b) review-pr poll discipline [bake permanently into git-pr-review.md's INITIAL subagent dispatch prompt: 'do NOT yield while the Copilot poll is pending — complete synchronously' (the subagent stalled/died mid-poll 4x). Encode the correct query: comment-author login is 'Copilot' but the review-REQUEST login is 'copilot-pull-request-reviewer'; GraphQL reviewRequests omits bot reviewers so confirm requests via REST requested_reviewers; orchestrator owns the wait (request review, background-poll 30s x20, dispatch only once review exists). Copilot lands ~4.5-6.5 min]. CONSTRAINTS: prose-only, no Go, no behavior-schema change; SPEC mirror per touched skill (docs/specs/skills/SPEC-*.md) — Copilot enforces this strictly; src/kit is canonical. SEQUENCING: develop in parallel with GROUP A [jznd] (disjoint surfaces: skill .md vs Go) but MERGE AFTER A. SRAD: likely passes gate (well-scoped prose) — /fab-fff candidate, /fab-clarify only if scope unclear.
- [ ] [5943] 2026-06-15: FKF Change 1/4 (feat, FOUNDATION — no deps): Add .status.yaml `summary:` field + migration. The C-lite log.md generator (FKF Change 2) reads this per-change one-line summary, so it MUST exist first. Tasks: (a) add `Summary string `yaml:"summary,omitempty"`` to StatusFile struct in src/go/fab/internal/statusfile/statusfile.go — decode in Load(), encode in syncToRaw() via insertKey(); (b) add `fab status set-summary <change> <text>` + `get-summary` CLI verbs (the conflict-free write path — each change touches only its own .status.yaml); (c) add `summary: ""` to template src/kit/templates/status.yaml; (d) migration src/kit/migrations/2.4.1-to-2.5.0.md adding the field to in-flight changes under fab/changes/ (skip archive/**, idempotent); bump src/kit/VERSION to 2.5.0. Docs: _cli-fab.md § fab status, SPEC-fab-status.md mirror, docs/specs/templates.md, memory pipeline/change-lifecycle.md + schemas.md. Graceful absence: a change with no summary projects its slug in the log. Spec: docs/specs/fkf.md §6.3. Context PR #419.
- [ ] [bmzo] 2026-06-15: FKF Change 2/4 (feat, KEYSTONE — depends on Change 1): `fab memory-index` emits per-folder log.md (C-lite) + stamps FKF frontmatter. Heaviest change; once in, the format physically exists. Package src/go/fab/internal/memoryindex/ (memoryindex.go, loss.go, indexparse.go, cmd memory_index.go). Tasks: (a) C-lite log.md generator — for each domain + sub-domain folder, join the EXISTING batched `git log --date=short --name-only -- docs/memory` pass with each change's .status.yaml `summary:`, group by date newest-first, emit one entry per commit-touching-file as: optional bold verb (Update/Creation/Deprecation, from change_type/removal) + bundle-relative link + summary + (change-id); see fkf.md §6.2 for exact format; (b) stamp `type: memory` frontmatter (template + round-trip in RenderDomain/RenderRoot); (c) write `fkf_version: "0.1"` into root index.md frontmatter (the only index allowed frontmatter); (d) extend --check/--json loss tiers (TierClean/BenignDrift/DestructiveLoss exit 0/1/2 in loss.go) to cover log.md + new frontmatter so refuse-before-regen guards keep working. Update _cli-fab.md § fab memory-index (lines ~447-538). Spec: docs/specs/fkf.md §5,§6,§8. Depends-on: Change 1 (summary field). Context PR #419.
- [ ] [8fr5] 2026-06-15: FKF Change 3/4 (refactor, depends on Change 2): Doc-skill prose → author FKF, stop writing per-file changelogs. Skills can now truthfully say 'history lives in generated log.md' because Change 2 emits it. Tasks: (a) src/kit/skills/fab-continue.md hydrate behavior — author `type:`+`description:` frontmatter, and CALL `fab status set-summary` instead of appending a `## Changelog` row; use bundle-relative memory↔memory links; (b) src/kit/skills/docs-hydrate-memory.md (all 3 modes: ingest/generate/backfill) — stamp `type: memory`, drop `## Changelog` from the file template, bundle-relative links; (c) src/kit/skills/_generation.md memory template; (d) _review.md if it touches changelogs. SYNCHRONOUS SPEC mirrors (Constitution rule): SPEC-fab-continue.md, SPEC-docs-hydrate-memory.md, SPEC-_generation.md, SPEC-_review.md. Memory: memory-docs/hydrate.md, memory-docs/templates.md, _shared/context-loading.md. NOTE: all 20 memory files currently DO carry `## Changelog` — skill prose writes them today; this change stops new writes (Change 4 strips existing). Spec: docs/specs/fkf.md §3.3,§7. Depends-on: Change 2. Context PR #419.
- [ ] [oovf] 2026-06-15: FKF Change 4/4 (refactor, CUTOVER — depends on Change 2 + Change 3, LAND LAST): Migrate the existing docs/memory/ tree to FKF + make reorg skills FKF-aware. Needs Change 2's generator and Change 3's writers stable first so the migrated tree matches what skills produce going forward. Tasks: (a) one-time conversion (migration file or a docs-reorg-memory orchestration pass) that strips the `## Changelog` table from all ~20 memory files, converts each existing changelog ROW into a seed log.md entry (DECISION b: preserve existing rows faithfully rather than collapsing pre-FKF history to git+slug — this is why we chose C-lite over pure-B; rich rows have no live .status.yaml to source from), converts all memory↔memory links from relative to bundle-relative (/...), ensures `type: memory` on every file, regenerates indexes + logs once; (b) src/kit/skills/docs-reorg-memory.md + docs-reorg-specs.md — handle FKF frontmatter during file moves, optionally add the generated-index borrow for docs/specs/ (fkf.md §9 optional — confirm with owner); (c) SPEC-docs-reorg-memory.md + SPEC-docs-reorg-specs.md mirrors. Verify no dangling links post-migration. Spec: docs/specs/fkf.md §10. Depends-on: Change 2 + Change 3. Context PR #419.
