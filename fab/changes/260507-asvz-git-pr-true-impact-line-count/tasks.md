# Tasks: git-pr true-impact line count

**Change**: 260507-asvz-git-pr-true-impact-line-count
**Spec**: `spec.md`
**Intake**: `intake.md`

<!--
  Source-of-truth note: all skill edits go to `src/kit/skills/git-pr.md`.
  Deployed copy at `.claude/skills/git-pr/SKILL.md` is regenerated by `fab sync`.
-->

## Phase 1: Setup

- [x] T001 [P] Add `true_impact_exclude: [fab/, docs/]` (YAML flow style, single-line) as a top-level entry to `src/kit/scaffold/fab/project/config.yaml` so new projects bootstrap with the field populated. <!-- clarified: flow style chosen to match the scenario examples in spec.md (e.g. "true_impact_exclude: [fab/, docs/]") and keep the scaffold concise -->
- [x] T002 [P] Add `true_impact_exclude: [fab/, docs/]` (YAML flow style, single-line) as a top-level entry to this project's own `fab/project/config.yaml` so the new behavior applies to the fab-kit repo itself once the migration ships. <!-- clarified: flow style for parity with T001 -->

> Note: T002 is intentionally redundant with the migration in T006 — applying the field directly here means the fab-kit repo benefits from the impact block on this very PR, before consumers run `/fab-setup migrations` on their own projects. <!-- clarified: rationale for T002 vs. just relying on the migration -->


## Phase 2: Core Implementation

- [x] T003 In `src/kit/skills/git-pr.md`, inside the existing Step 3c, insert a new sub-step **between current sub-step 3 ("Generate PR body") and sub-step 4 ("Create PR")** — number it 3c-impact (or extend the numbered list as 3.5). The sub-step SHALL: <!-- clarified: precise insertion point — Step 3c currently has 5 numbered sub-steps; the new logic must run after body generation but before `gh pr create`, so it can append the impact block to the assembled body string -->
  1. Reads `true_impact_exclude` from `fab/project/config.yaml` via `yq` (only when `{has_fab}` is true).
  2. If the list is null/missing/empty, skip — no block, no extra git invocation.
  3. Otherwise, computes `BASE=$(git merge-base origin/main HEAD)` fresh in this sub-step (the current `/git-pr` does NOT compute a merge-base anywhere else, so there is no existing variable to reuse), then runs two `git diff --shortstat "$BASE...HEAD"` invocations: one with `-- . ':(exclude)<pat>' ...` for the true-impact pass and one without exclusions for the total pass. <!-- clarified: spec/intake said "already computed in /git-pr" but git-pr.md has no merge-base call today — T003 must introduce it -->
  4. Parses each `--shortstat` line for `(\d+) insertions?\(\+\)` and `(\d+) deletions?\(-\)`; missing clauses default to `0`.
  5. If the true-impact pass yields `+0 / −0`, omits the entire block.
- [x] T004 In the same `src/kit/skills/git-pr.md` Step 3c sub-step, define the rendered impact block format:
  ```markdown

  **Impact (excluding {COMMA_LIST})**: +A / −D
  **git diff total**: +A_total / −D_total
  ```
  where `{COMMA_LIST}` is `true_impact_exclude` joined with `, ` (literal comma + space). Append it to the PR body **after** the pipeline progress line (the current last content) with a single blank line separator. Do NOT wrap in a `## Impact` heading.

## Phase 3: Integration & Edge Cases

- [x] T005 In `src/kit/skills/git-pr.md` Step 3c, document the explicit fallbacks in prose: (a) absent / null / empty list → block omitted, (b) `{has_fab}` is false → block omitted, (c) true-impact pass yields zero changes → block omitted. Cross-reference the spec's degenerate-state assumptions (#11, #12).
- [x] T006 Create `src/kit/migrations/1.9.1-to-1.9.2.md` (using the next-patch-after-`src/kit/VERSION` convention — current is `1.9.1`, target is `1.9.2`) following the existing migration format (Summary / Pre-check / Changes / Verification — see `src/kit/migrations/1.8.0-to-1.9.0.md` for the canonical structure). Behavior: idempotent no-op when `true_impact_exclude` is already a top-level key in `fab/project/config.yaml`; otherwise append `true_impact_exclude: [fab/, docs/]` (flow style, matching T001/T002) as a new top-level entry. Atomic write (temp + rename). Skip silently when `fab/project/config.yaml` does not exist (project not initialized). <!-- clarified: flow style chosen for consistency with T001/T002 and the spec scenarios; previous draft used block style which would have diverged from the scaffold default -->
- [x] T007 Bump `src/kit/VERSION` from `1.9.1` to `1.9.2` to align the engine version with the migration's `{TO}` filename component. This task only concerns the source `VERSION` file — the `docs/memory/fab-workflow/migrations.md` changelog entry is updated at hydrate, not here. <!-- clarified: removed the contradictory "Update the migrations.md memory changelog hint inline" phrase from the prior draft; this task is VERSION-only -->


## Phase 4: Polish

- [x] T008 Update `docs/specs/skills/SPEC-git-pr.md` to document the new sub-step in the flow diagram (insert "3c-impact" between Step 3c and Step 4a) and add a one-line note in the Tools-used / Sub-agents block describing the two `git diff --shortstat` reads. Per the constitution: any change to a skill MUST update the corresponding `SPEC-*.md`.

## Execution Order

- T001 and T002 are independent ([P]) — both edit different config files (`src/kit/scaffold/fab/project/config.yaml` vs. `fab/project/config.yaml`).
- T003 must precede T004 (T004 references the sub-step T003 establishes in `src/kit/skills/git-pr.md`).
- T005 builds on the prose introduced in T003–T004 (same file).
- T006 and T007 can run in either order in practice — both target the `1.9.2` version. The recommended order is T006 then T007 so the migration file exists at the moment `VERSION` is bumped, but neither task blocks the other technically. <!-- clarified: prior draft over-stated the dependency; the filename `1.9.1-to-1.9.2.md` is determined by reading current VERSION (1.9.1), not by VERSION's post-bump value -->
- T008 (spec doc update) is final — written after the skill changes settle to avoid repeated edits.

All file paths referenced in T001–T008 have been verified to exist (or, for T006, to follow the established `src/kit/migrations/{FROM}-to-{TO}.md` naming convention seen in sibling migrations like `1.8.0-to-1.9.0.md`). <!-- clarified: explicit file-path validation note added during fab-clarify auto scan -->
