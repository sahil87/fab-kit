# Plan: Split true-impact line count by implementation vs. tests

**Change**: 260530-7t5a-true-impact-test-split
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at the apply stage entry. Apply parses
  `## Tasks` only; review parses `## Acceptance` only. Section headings are the
  stable parser contract.
-->

## Tasks

### Phase 1: Setup (config + engine plumbing)

- [x] T001 Add `TestPaths []string` field (`yaml:"test_paths"`) to `Config` in `src/go/fab/internal/config/config.go`, mirroring the existing `TrueImpactExclude` field.

### Phase 2: Core Implementation (engine)

- [x] T002 In `src/go/fab/internal/impact/impact.go`: add `Tests *Pair` to `Result`; extend `runShortstat` to accept include pathspecs (in addition to the existing excludes); add `testPaths []string` parameter to `Compute` and run the test-only `git diff --shortstat` pass (test includes AND `true_impact_exclude` excludes both applied) within the scaffolding-excluded universe, populating `Result.Tests` (nil when `testPaths` empty); update `ComputeForRepo` to read `cfg.TestPaths` and thread it through. Update the package/function doc comments.
- [x] T003 [P] Tests in `src/go/fab/internal/impact/impact_test.go`: test-only pass counts test lines within the excluded universe; empty-`testPaths` → `Tests` nil + no extra pass; empty-`true_impact_exclude` → tests split within raw universe; engine does NOT clamp (over-counting tests leaves a negative residual computable by caller, but engine stores raw measured passes); fix existing `Compute(...)` callers in tests for the new signature.

### Phase 3: Status persistence + CLI surface

- [x] T004 In `src/go/fab/internal/statusfile/statusfile.go`: add `Tests *TrueImpactPair` (`yaml:"tests,omitempty"`) to `TrueImpact` after `Excluding`, before `ComputedAt`; update `encodeTrueImpact` to emit the `tests` mapping after `excluding`, before `computed_at` (decode is tag-driven, already works).
- [x] T005 [P] Tests in `src/go/fab/internal/statusfile/statusfile_test.go`: encode emits `tests` after `excluding` / before `computed_at`; round-trip decode of `tests`; omitempty (nil `Tests` → no `tests:` key); absent-`tests` back-compat (old file w/o `tests` parses + re-saves unchanged).
- [x] T006 In `src/go/fab/internal/status/true_impact.go`: `WriteTrueImpact` copies `res.Tests` into `ti.Tests` (mirror existing `res.Excluding` handling). Stores only measured passes — no derived `impl`.
- [x] T007 In `src/go/fab/cmd/fab/impact.go`: `renderYAML` emits the `tests:` sub-block (added/deleted/net) after `excluding` and before `computed_at`, only when `r.Tests` is non-nil.

### Phase 4: Render-time consumers (clamp + equation)

- [x] T008 In `src/go/fab/internal/change/change.go`: `impactColumn()` renders the compact explicit-equation form `{impl_net}i+{tests_net}t={total_net}` when `ti.Tests` present, where total=`Excluding.Net` (else raw `Net`), tests=`Tests.Net`, impl=`max(0, total−tests)` (per-net clamp). Bare-net fallback when `Tests` absent.
- [x] T009 [P] Tests in `src/go/fab/internal/change/change_test.go`: compact equation form; net-clamp guard (`0i+450t=400`); bare-net fallback without `tests`.

### Phase 5: Skill rendering + docs

- [x] T010 In `src/kit/skills/git-pr.md`: replace the single inline `**Impact**` line with the three-row block (impl/tests/total) when `tests` present; total=scaffolding-excluded pass (fallback raw), impl=per-component `max(0, total.X − tests.X)`, `(excludes …)` from actual config, raw NOT shown, Unicode minus `−`; one-line stderr warning on any per-component clamp; never render negative impl; fall back to today's excluding-only rendering when `tests` absent. Update the YAML-parse step to also read the `tests` sub-block.
- [x] T011 [P] In `src/kit/scaffold/fab/project/config.yaml`: add a COMMENTED-OUT `test_paths` placeholder (no active patterns) documenting attribution-not-exclusion, language-specific / no kit default, graceful collapse, and example patterns.
- [x] T012 [P] In `src/kit/skills/_cli-fab.md`: document the new `tests` sub-block in the `fab impact` output schema (after `excluding`, before `computed_at`, emitted only when `test_paths` non-empty).
- [x] T013 [P] In `docs/specs/skills/SPEC-git-pr.md`: document the three-row Impact rendering and the total-semantics change (total = scaffolding-excluded; raw not shown; impl = residual; tests sub-block parsed from `fab impact`).
- [x] T014 [P] In this repo's `fab/project/config.yaml`: add `test_paths: ["**/*_test.go"]` so the feature is exercised on its own PR (config edit, not scaffold).

### Phase 6: Final verification

- [x] T015 Run `go build ./...` and `go test ./...` from `src/go/fab/`; fix any failures.

## Execution Order

- T001 → T002 (Compute reads config); T002 → T003 (tests need new signature).
- T002 → T006, T007 (consumers of `Result.Tests`); T004 → T005, T006 (status persistence).
- T004 → T008 (impactColumn reads `TrueImpact.Tests`); T008 → T009.
- T010–T014 are doc/skill edits, independent of each other and of the Go build.
- T015 last (depends on all Go code tasks).

## Acceptance

### Functional Completeness

- [ ] A-001 `test_paths` config field: `config.Config` has `TestPaths []string` (`yaml:"test_paths"`); `config.Load` populates it from the top-level `test_paths` key; empty/null/absent yields empty/nil without error.
- [ ] A-002 Engine `Result.Tests`: `impact.Result` has a `Tests *Pair` field; no derived `impl`/residual field anywhere in the engine.
- [ ] A-003 Test-only shortstat pass: `Compute(repoDir, base, head, excludes, testPaths)` runs an extra `git diff --shortstat` pass with BOTH test includes and `true_impact_exclude` excludes when `testPaths` non-empty; `runShortstat` accepts include pathspecs; `ComputeForRepo` reads `cfg.TrueImpactExclude` and `cfg.TestPaths`.
- [ ] A-004 Engine stores only measured passes: raw, `Excluding`, `Tests` — no clamp, no residual in the engine.
- [ ] A-005 `statusfile.TrueImpact` gains `Tests *TrueImpactPair` (`yaml:"tests,omitempty"`); `encodeTrueImpact` emits `tests` after `excluding`, before `computed_at`; omitted when nil.
- [ ] A-006 `WriteTrueImpact` copies `res.Tests` into `ti.Tests` (mirrors `res.Excluding`); best-effort stderr posture preserved; stores no derived `impl`.
- [ ] A-007 `renderYAML` emits the `tests:` sub-block after `excluding` and before `computed_at` when `r.Tests` non-nil; omits it when nil (byte-compatible with today).
- [ ] A-008 `/git-pr` three-row Impact block when `tests` present (impl/tests/total); total = scaffolding-excluded (fallback raw); impl = per-component `max(0, total.X − tests.X)`; `(excludes …)` from actual config; raw not shown; Unicode minus; bare/single-line fallback when `tests` absent.
- [ ] A-009 `impactColumn` compact equation `{impl_net}i+{tests_net}t={total_net}` when `Tests` present (total=`Excluding.Net` else raw `Net`; impl=`max(0,total−tests)`); bare-net fallback (`Excluding.Net` else `Net` else `—`) when `Tests` absent.
- [ ] A-010 Scaffold config has a commented-out `test_paths` placeholder (no active default patterns) with attribution/language-specific/collapse explanation and examples.
- [ ] A-011 Docs updated: `_cli-fab.md` documents the `tests` sub-block in `fab impact` output; `SPEC-git-pr.md` describes the three-row rendering + total-semantics change.

### Behavioral Correctness

- [ ] A-012 Total-semantics change in PR body: the `total` row is the scaffolding-excluded number (today's `excluding`), NOT the raw with `fab/`/`docs/`; raw is never displayed in the PR body.
- [ ] A-013 `impl = total − tests` derived at render time only (both `/git-pr` and `impactColumn`); never read from a stored field.

### Edge Cases & Error Handling

- [ ] A-014 Empty `test_paths`: no `tests` pass, `Result.Tests` nil, `.status.yaml`/`fab impact` omit `tests`, render collapses to today's single-number behavior.
- [ ] A-015 Empty `true_impact_exclude`: total degenerates to raw (no `excluding`), tests still split within the raw universe when `test_paths` set.
- [ ] A-016 Test fixture under an excluded path is not double-counted (exclude wins in the test pass).
- [ ] A-017 Per-component negative-impl clamp: `impl.X = max(0, total.X − tests.X)` independently for added/deleted/net; one-line stderr warning on any clamp; no negative component rendered (PR body + compact column).

### Scenario Coverage

- [ ] A-018 Engine tests cover: test-only pass within excluded universe, empty-`testPaths` (Tests nil), empty-`true_impact_exclude` (split within raw), engine-does-not-clamp.
- [ ] A-019 `statusfile` tests cover: encode order (`added,deleted,net,excluding,tests,computed_at,computed_at_stage`), round-trip decode of `tests`, omitempty, absent-`tests` back-compat.
- [ ] A-020 `change` tests cover: compact equation form, net-clamp guard, bare-net fallback.

### Code Quality

- [ ] A-021 Pattern consistency: new code follows the existing `Excluding`/`runShortstat`/`encodeTrueImpact` patterns (naming, error handling, structure).
- [ ] A-022 No unnecessary duplication: `runShortstat` is reused for the test pass (not a new helper); no God functions; no magic strings.

### Documentation Accuracy

- [ ] A-023 `_cli-fab.md` and `SPEC-git-pr.md` accurately reflect the shipped behavior (schema field order, total semantics, omit rules); canonical edits under `src/`, never `.claude/skills/`.

### Cross References

- [ ] A-024 The `tests` sub-block field order and omit semantics are consistent across `.status.yaml`, `fab impact` YAML, and the docs; `(excludes …)` annotation always reflects actual `true_impact_exclude` config values.

## Notes

- Memory files (`schemas`, `configuration`) are updated at HYDRATE, not apply — do not touch `docs/memory/`.
- Do NOT run `fab status finish` or `fab sync` — the orchestrator handles transitions.
