# Plan: {CHANGE_NAME}

**Change**: {YYMMDD-XXXX-slug}
**Intake**: `intake.md`

<!--
  AUTO-GENERATED by /fab-continue (or /fab-ff/fab-fff) at the apply stage entry.

  This single file is co-generated in one pass and carries THREE sections that
  the pipeline consumes:

    - ## Requirements — the requirement discipline absorbed from the former
      spec.md: RFC-2119 statements (MUST/SHALL/SHOULD/MAY) with stable R# IDs,
      each with at least one GIVEN/WHEN/THEN scenario. Generated FIRST, from the
      intake-derived design. Review re-reads this against the implementation.
    - ## Tasks — imperative work items. Apply parses this section (and only this
      section) during execution.
    - ## Acceptance — declarative outcomes. Review parses this section (and only
      this section) to verify the change.

  Section headings (`## Requirements`, `## Tasks`, `## Acceptance`) and the
  R#/T{NNN}/A-{NNN} ID formats are the stable parser contract. Phase/category
  subheadings under each are presentational and may vary per change.

  TRACEABILITY (REQUIRED): the chain R# (requirement) → T# (task) → test →
  A# (acceptance) lets the autonomous (apply ↔ review) loop localize a failing
  acceptance item back to its requirement and forward to the task that satisfies
  it. Therefore:
    - Each ## Tasks item MUST carry a `<!-- R# -->` trace annotation naming the
      requirement it implements.
    - Each requirement-derived ## Acceptance item MUST name the requirement it
      accepts (e.g., `A-001 R2: {outcome}`). Non-requirement-derived categories
      (Code Quality, checklist.extra_categories) carry no R#:
      `A-{NNN}: {outcome}` (an optional label may precede the colon).

  NO [NEEDS CLARIFICATION] MARKERS: those are an intake-only construct (they mean
  "a human still needs to decide this", and humans only decide at intake). An
  under-specified requirement encountered at apply is resolved inline as a graded
  SRAD assumption (Certain/Confident/Tentative) recorded in ## Assumptions — never
  as a marker in this file.

  TASK FORMAT: - [ ] T{NNN} [{markers}] {Description with file paths} <!-- R# -->

  Markers (optional, combine as needed):
    [P]   — Parallelizable (different files, no dependencies on other [P] tasks in same group)

  IDs are sequential: T001, T002, ...
  Include exact file paths in descriptions.
  Each task should be completable in one focused session.

  Tasks are grouped by phase. Phases execute sequentially.
  Within a phase, [P] tasks can execute in parallel.

  ACCEPTANCE FORMAT (requirement-derived categories): - [ ] A-{NNN} R#: {declarative outcome}
  ACCEPTANCE FORMAT (Code Quality / extra categories):  - [ ] A-{NNN}: {declarative outcome}

  IDs are sequential and zero-padded: A-001, A-002, ...
  Acceptance items are declarative ("X is done and correct"), not imperative.
  Review marks each item [x] in place when verified.

  REVIEW-OWNED PARSER CONTRACT: ## Deletion Candidates

  The review stage may append a top-level `## Deletion Candidates` section
  below `## Notes` (or at end of file when `## Notes` is absent). Hydrate
  reads it informationally; absence is treated as "no findings" with no
  error. Both the parsimony pass and the deletion-candidate prompt are
  silently skipped when change_type is `docs`, `chore`, or `ci`, in which
  case the section is omitted entirely (not written as "None"). Do NOT add
  a placeholder section here — the section is review-generated, not
  template-scaffolded.
-->

## Requirements

<!-- Absorbed from the former spec.md. Requirements use RFC 2119 keywords:
     MUST/SHALL (mandatory), SHOULD (recommended), MAY (optional). Every
     requirement MUST have at least one GIVEN/WHEN/THEN scenario and a stable
     R# ID. Organize by domain section when the change touches multiple domains.
     NO [NEEDS CLARIFICATION] markers — record under-specified points as graded
     ## Assumptions rows instead.

     OPTIONAL SUBSECTIONS — add any when needed, omit entirely when not:
       ### Non-Goals
       Each entry: - {what is excluded} — {brief reason, if not obvious}
       ### Design Decisions
       Each entry uses the four-field shape (matching the memory ## Design Decisions
       shape, FKF §3.3, so hydrate lifts it into memory without reshaping):
         **Decision**: {approach}
         **Why**: {rationale}
         **Rejected**: {alternative and why it was worse}
         *Introduced by*: {change-name}
       ### Deprecated Requirements
       Include only when this change removes existing requirements. Each entry:
         #### {Requirement Name}
         **Reason**: {why removed}
         **Migration**: {what replaces it, or "N/A"}
-->

### {Domain}: {Topic}

#### R1: {Requirement Name}
{Requirement text using SHALL/MUST/SHOULD/MAY}

- **GIVEN** {precondition}
- **WHEN** {action or event}
- **THEN** {expected outcome}
- **AND** {additional outcome, if needed}

#### R2: {Another Requirement}
{Requirement text}

- **GIVEN** {precondition}
- **WHEN** {action}
- **THEN** {outcome}

## Tasks

<!-- Sequential work items for the apply stage. Checked off [x] as completed.
     Each item MUST carry a <!-- R# -->  trace annotation. -->

### Phase 1: Setup

<!-- Scaffolding, dependencies, configuration. No business logic. -->

- [ ] T001 {setup task with file path} <!-- R1 -->
- [ ] T002 [P] {parallel setup task} <!-- R1 -->

### Phase 2: Core Implementation

<!-- Primary functionality. Order by dependency — earlier tasks are prerequisites for later ones. -->

- [ ] T003 {implementation task referencing specific file path} <!-- R2 -->
- [ ] T004 {task that depends on T003} <!-- R2 -->
- [ ] T005 [P] {independent task} <!-- R1 -->

### Phase 3: Integration & Edge Cases

<!-- Wire components together. Handle error states, edge cases, validation. -->

- [ ] T006 {integration task} <!-- R2 -->
- [ ] T007 {error handling task} <!-- R2 -->

### Phase 4: Polish

<!-- Documentation, cleanup, performance. Only include if warranted by the change scope. -->

- [ ] T008 {polish task} <!-- R1 -->

## Execution Order

<!-- Summary of non-obvious dependencies between tasks. Omit if order is self-evident. -->

- T003 blocks T004
- T005 is independent, can run alongside T003-T004

## Acceptance

<!-- Declarative acceptance criteria used by the review stage. Items are derived from:
       1. ## Requirements — every requirement should have coverage
       2. Plan decisions — technical choices should be reflected in code
       3. Project constitution — project-wide quality standards
     Each requirement-derived item MUST name the requirement it accepts (e.g., A-001 R1: ...).
     Code Quality and checklist.extra_categories items carry no R# (e.g., A-007 Pattern consistency: ...).
     /fab-continue (review) checks all items. ALL must pass before hydrate. -->

### Functional Completeness

<!-- Every requirement in ## Requirements has working implementation. -->

- [ ] A-001 R1: {specific verifiable criterion}
- [ ] A-002 R2: {criterion}

### Behavioral Correctness

<!-- Changed requirements behave as specified, not as before. -->

- [ ] A-003 R2: {what changed and how to verify}

### Removal Verification

<!-- Every deprecated requirement is actually gone. -->

- [ ] A-004 R#: {confirm removed, no dead code}

### Scenario Coverage

<!-- Key scenarios from ## Requirements have been exercised. -->

- [ ] A-005 R#: {how to verify — test exists, manual check, etc.}

### Edge Cases & Error Handling

<!-- Error states, boundary conditions, failure modes. -->

- [ ] A-006 R#: {what should happen}

### Code Quality

<!-- Always included. Baseline items when no code-quality config; expanded when config exists. -->

- [ ] A-007 Pattern consistency: New code follows naming and structural patterns of surrounding code
- [ ] A-008 No unnecessary duplication: Existing utilities reused where applicable
<!-- When fab/project/code-quality.md exists, add one item per relevant principle and anti-pattern. -->

### Security

<!-- Only include if the change has security surface. -->

- [ ] A-009 R#: {verification}

## Notes

- Check items as you review: `- [x]`
- All acceptance items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] A-NNN **N/A**: {reason}`

## Assumptions

<!-- SCORING SOURCE NOTE: as of 1.10.0, `fab score` reads intake.md only — this
     ## Assumptions section is the apply-agent's record of graded decisions made
     while co-generating ## Requirements (under-specified points resolved inline),
     NOT a scoring source. Three grades only (Certain/Confident/Tentative) —
     Unresolved is intake-only; apply decides and records, it never leaves a
     decision Unresolved. The Scores column is required for every row. -->

| # | Grade | Decision | Rationale | Scores |
|---|-------|----------|-----------|--------|
| 1 | {Certain|Confident|Tentative} | {decision summary} | {why this grade} | S:nn R:nn A:nn D:nn |

{N} assumptions ({Ce} certain, {Co} confident, {T} tentative).
