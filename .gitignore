.fab-status.yaml
.fab-backend
.fab-runtime.yaml
# Lock siblings for the shared-state flock guards (mz4q): .fab-runtime.yaml.lock
# at each worktree root, .status.yaml.lock next to each fab/changes/*/.status.yaml
.fab-runtime.yaml.lock
.status.yaml.lock
# fab/backlog.md
/.agents
/.claude
/.cursor
/.opencode
/.codex
/.kimi
# Kept after the gemini provider was retired: this checkout still carries an
# untracked .gemini/skills/ tree from before, and un-ignoring it would surface
# ~36 stray files in `git status`. The scaffold fragment dropped the entry —
# user projects merge additively (lineEnsureMerge), so theirs is unaffected.
/.gemini

/ToBeDeleted/
/dist/
.tmp
src/go/fab/wt

# Rust build artifacts
src/rust/fab/target/

# Gas Town runtime
.beads/
.runtime/
.logs/

.fab-*
!fab/.fab-version
.uploads/
