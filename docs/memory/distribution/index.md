---
description: "How the kit is packaged, shipped & configured — .kit/ structure, three-binary architecture, Homebrew, releases, migrations, /fab-setup bootstrap"
---
# Distribution Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [distribution](distribution.md) | How src/kit/ is distributed — Homebrew formula (fab router + fab-kit direct; wt/idea standalone siblings, no depends_on), always-route policy, fab init bootstrap, fab upgrade-repo (offline-first, --latest opt-in), release workflow, auto-download (timeouts, flock, atomic rename, digest verify), fail-loud exit contracts (exit 3 = not-a-fab-managed-repo), update brew-safety, the shll.ai README/docs-site pull surface, toolkit-standards conformance (incl. install-composition: install docs on shll.ai). |
| [kit-architecture](kit-architecture.md) | src/kit/ structure (binary-free, content-only) and the three-binary architecture — fab router (always-route policy, shared LifecycleCommands allowlist), fab-kit workspace lifecycle (sync pipeline, config generation, ExitNotManaged=3), and the fab-go workflow engine. Covers agent skill deployment, version tracking (fab/.fab-version), the fab config group + loader cascade, fab dispatch, fab skill, help-dump, the binary-wide 0/1/2 exit-code convention, the internal/ package map, and the wt binary. |
| [migrations](migrations.md) | The migration system — dual-version model (engine VERSION vs fab/.kit-migration-version), the {FROM}-to-{TO}.md file format, range-based applicability, binary-owned discovery (fab migrations-status [--json] / DiscoverMigrations; exit 3 = not-a-fab-managed-repo, 1 = genuine error), the /fab-setup migrations runner (discovery delegated, application LLM-driven), version-drift detection, and the shipped migration catalog. |
| [setup](setup.md) | The /fab-setup skill — structural bootstrap (sync-first order: doctor → config → constitution → fab sync), its subcommand architecture (config, constitution, migrations), delegation to fab-kit sync, and Config Create-Mode (generates config.yaml via fab config init --project with a detected identity seed + test_paths marker detection). Covers the fail-loud scaffold merge and the gitignore-aware .gitignore dedup. |
