---
description: "Agent & process runtime — fab pane primitives + the `@rk_agent_state` tmux pane-option agent-state reader (agent-state production divested to run-kit; `.fab-runtime.yaml` deleted) + the operator coordination skill (tmux-bound interactive path), and fab dispatch, the two-mode process manager for CLI-dispatched pipeline stages (headless, or a tmux-pane worker in a carved worker column, resolved per invocation)"
---
# Runtime Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [agent-primitives](agent-primitives.md) | The `_cli-agents` helper — agent-CLI interaction primitives any skill opts into via `helpers:`: spawn composition from `fab agent --print` (role- or provider-addressed; the provider form bypasses role resolution AND the fills), the pane-exists/agent-idle pre-send gate, the printed-prompt probe, peek (capture + `@rk_agent_state`; uninstrumented panes read unknown), await (poll or announce), and a grammar + discovery-recipe dictionary for the four built-ins plus the `codex mcp-server` bridge. |
| [dispatch](dispatch.md) | `fab dispatch {start,open,ready,deliver,restart,status,wait,logs,kill,reap,clean}` manages headless and pane stage workers. `start` launches headless; pane mode enters at `open`, is probed by `ready`, and is handed its prompt by the verified `deliver` send-keys choreography (also the pane-arm resume). Automatic launch descends dispatch.mode's pane → native → headless ceiling; native redirects to the harness before state writes. Covers state layout, observation, recovery, placement, and reaping. |
| [operator](operator.md) | Operator coordination skill (`/fab-operator`) — multi-repo / multi-session coordination on one tmux server via the `(session, repo, pane)` addressing tuple and one server-keyed state file: the non-blocking strategic auto-answer model, an adaptive heartbeat keyed off the `@rk_agent_state` Agent column, repo-targeted spawning, two-tier (same-repo cherry-pick / cross-repo ordering-only) dependency resolution, repo-spanning autopilot, the status frame, and the operator-role launcher. |
| [pane-commands](pane-commands.md) | `fab pane {map,capture,send,process,window-name}` subcommand reference, persistent `--server`/`-L` flag, the unified pane-family exit-code scheme (2 = pane missing / 3 = other tmux failure), shared `internal/pane` helpers + the targeted `display-message` pane-validation probe, pane map `display_state` + stable `window_id` JSON fields, pane-ID-per-server semantics, the three-axis model (Change / Agent / Process), and window-name primitives for guarded tmux window rewrites. |
| [providers-and-profiles](providers-and-profiles.md) | The providers & agent-roles model — `agent.session`/`agent.workers` depth knobs, per-session `FAB_AGENT_SESSION`/`FAB_AGENT_WORKERS` overrides and `--workers` launch sugar; the `providers:` table as pure capability grammar (`interactive_command`, `native`, `headless_command`) with per-role fills; six fixed roles, fill precedence, stage→role mapping, `fab resolve-agent`, `fab agent`, the `dispatch.mode` preference descent, and consumers. |
| [runtime-agents](runtime-agents.md) | Agent-state divestment: fab reads the `@rk_agent_state` tmux pane option (`state:epoch_seconds`; states active/waiting/idle, absent = unknown) via plain tmux commands — a data convention, NOT a run-kit software dependency; run-kit's `rk agent-setup` is the writer. The former `.fab-runtime.yaml` `_agents` PRODUCER pipeline (hooks/GC/PID-walker/flock/`internal/runtime`+`internal/proc`) is deleted; fab is now a pure consumer, via the pure `parseAgentState` parser + surviving `FormatIdleDuration`. |
