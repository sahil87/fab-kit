# Log — Memory Docs Documentation
<!-- Generated by `fab memory-index` from git history + per-change summaries. Do not hand-edit. -->

## 2026-06-16
- **Update** [hydrate](/memory-docs/hydrate.md) — refactor: FKF Change 4/4 — Migrate docs/memory/ Tree to FKF + FKF-Aware Reorg Skills (#425)
- **Update** [hydrate](/memory-docs/hydrate.md) — refactor: FKF Change 3/4 — doc-skill prose authors FKF frontmatter, stops per-file changelog writes (#423)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — refactor: FKF Change 4/4 — Migrate docs/memory/ Tree to FKF + FKF-Aware Reorg Skills (#425)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — refactor: FKF Change 4/4 — Migrate docs/memory/ Tree to FKF + FKF-Aware Reorg Skills (#425)
- **Update** [specs-index](/memory-docs/specs-index.md) — refactor: FKF Change 4/4 — Migrate docs/memory/ Tree to FKF + FKF-Aware Reorg Skills (#425)
- **Update** [templates](/memory-docs/templates.md) — refactor: FKF Change 4/4 — Migrate docs/memory/ Tree to FKF + FKF-Aware Reorg Skills (#425)
- **Update** [templates](/memory-docs/templates.md) — refactor: FKF Change 3/4 — doc-skill prose authors FKF frontmatter, stops per-file changelog writes (#423)

## 2026-06-15
- **Update** [hydrate](/memory-docs/hydrate.md) — feat: extend fab memory-index --check with severity exit-code tiers and --json flag (#416)
- **Update** [hydrate](/memory-docs/hydrate.md) — feat: docs-reorg-memory orchestrates frontmatter backfill for pre-fab-kit trees (#415)
- **Update** [hydrate](/memory-docs/hydrate.md) — **Refuse-before-regen guard + mechanical reorg detection** (direct follow-on to 5ewp). New `### Refuse-Before-Regen Guard` section: every `fab memory-index` regeneration site in this skill first consults `fab memory-index --check` and **refuses to regenerate on exit 2** (destructive loss — curated description→`—`, tombstone drop, or grouping flatten), surfacing the `→ run /docs-reorg-memory to remediate ...` pointer (the orchestrator that relocates tombstone rows and dispatches `/docs-hydrate-memory` backfill mode for descriptions; backfill alone does not relocate tombstones). As the primary pre-fab-kit-tree entry point, the guard protects the *first* regen of a legacy tree. **No-op on born-compatible trees** (provably never exit 2) — annotated so it is not mistaken for dead code. The reorg orchestration seam note updated: reorg now detects the compatibility gap **mechanically via `fab memory-index --check --json`** (older-binary prose fallback retained) instead of prose-eyeballing — single source of truth, the loss logic lives in Go. Cross-references the two sibling guard sites (reorg via its `--check --json` detection; `/fab-continue` hydrate defense-in-depth). (glwc)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — feat: docs-reorg-memory orchestrates frontmatter backfill for pre-fab-kit trees (#415)
- **Update** [specs-index](/memory-docs/specs-index.md) — feat: docs-reorg-memory orchestrates frontmatter backfill for pre-fab-kit trees (#415)
- **Update** [templates](/memory-docs/templates.md) — feat: FKF Change 2/4 — fab memory-index emits per-folder log.md + stamps FKF frontmatter (#422)
- **Update** [templates](/memory-docs/templates.md) — feat: extend fab memory-index --check with severity exit-code tiers and --json flag (#416)
- **Update** [templates](/memory-docs/templates.md) — feat: docs-reorg-memory orchestrates frontmatter backfill for pre-fab-kit trees (#415)
- **Update** [templates](/memory-docs/templates.md) — **Memory File Format + Index Hierarchy gain the generated FKF surface** (FKF KEYSTONE, change 2/4). `description:` frontmatter bullet now notes the FKF `type: memory` constant and that bmzo's `fab memory-index` provides only the **preserve-when-present round-trip mechanism** (no authoring/bulk-stamp — those are FKF 3/4 and 4/4; most existing files do not yet carry `type:`, benign). The **Changelog** bullet + Hydration Rule 5 clarified: the per-file `## Changelog` table is **still hand-maintained today** — FKF's generated `log.md` is the eventual replacement but the strip is a later FKF change (not bmzo). Root `index.md` bullet documents the `fkf_version: "0.1"` root-only frontmatter (FKF §8, byte-stable, benign-drift on `--check`). New `#### Generated log.md (FKF C-lite — bmzo)` subsection: the per-folder single-writer `log.md` shape (§6.2 format with worked example), the git+`summary:` C-lite join, optional verb derivation, bundle-relative links (§7), slug fallback (§6.3), registry-gated change-id join with graceful degradation (≈0 ids on this repo's squash history → commit-subject fallback), empty-folder skip, and tier-1 `--check` treatment. Index Hierarchy intro reconciled to the `--name-status` batched pass (widened in bmzo to feed the log); Hydration Rule 4 now names `log.md` + `fkf_version` as generated outputs. (bmzo)
- **Update** [templates](/memory-docs/templates.md) — **Reorg compatibility detection rewired to MECHANICAL** (direct follow-on to 5ewp). The Memory Tree Shape reorg-orchestration paragraph now states that `/docs-reorg-memory` detects the pre-fab-kit compatibility gap by **calling `fab memory-index --check --json`** rather than the agent prose-eyeballing the tree: exit 0/1 = nothing to relocate/backfill (Compatibility report skipped); exit 2 = `losses[]` enumerates each divergence by `category` (`description`/`tombstone`/`grouping`) with `path` + `detail`. This rewires 5ewp's three prose signals to the Go primitive (single source of truth; no prose re-stating `frontmatter.Field` / tombstone / flatten semantics). Older-binary fallback retains the legacy prose detection + an upgrade warning. The on-approval orchestration (relocate tombstones → dispatch backfill → single regen) is unchanged in behavior — it now consumes the primitive's structured output; tombstones still user-confirmed before relocation. (glwc)

## 2026-06-14
- **Update** [hydrate](/memory-docs/hydrate.md) — **Backfill mode added** (third mode beside ingest + generate). Migrates a pre-fab-kit hand-curated tree to the convention `fab memory-index` depends on by adding missing `description:` frontmatter — body-preserving (only prepends/edits frontmatter), independent re-scan of `docs/memory/` (no caller manifest; same `frontmatter.Field` semantics), synthesis preferring a file-by-file-mapping curated index row, idempotent skip of frontmatter-present files, stub-before-index creation of missing domain/sub-domain index stubs, and caller-aware regen deferral (skips `fab memory-index` when reorg-dispatched; runs it when invoked directly). Reachable via the explicit `backfill` keyword or a `/docs-reorg-memory` dispatch. Captures the reorg orchestration seam: reorg detects the compatibility gap, relocates tombstones to `_shared/removed-domains.md`, dispatches backfill, then runs the single regen — per-file synthesis lives in backfill, reorg's job stays structural. Direct-user backfill does NOT detect/relocate tombstones (reorg-only). (5ewp)
- **Update** [specs-index](/memory-docs/specs-index.md) — **No compatibility/backfill step for specs** recorded (new Requirements subsection): specs are out of scope for the pre-fab-kit frontmatter backfill `/docs-reorg-memory` orchestrates — verified asymmetry (no specs-index generator, hand-rewritten index, Constitution VI human-curated). `docs-reorg-specs.md` now carries an explicit one-line no-symmetry note so a future contributor does not "fix the asymmetry" by adding a specs backfill. (5ewp)
- **Update** [templates](/memory-docs/templates.md) — **Memory Tree Shape gains `docs-reorg-memory`'s pre-fab-kit compatibility orchestration**: beyond the file-moving rebalance, reorg is the single front door for migrating a hand-curated tree to the convention. Its read-all-files diagnosis now runs compatibility detection (topic files missing `description:` frontmatter; tombstone rows = index rows whose `docs/memory/`-relative link target is absent on disk, strikethrough a corroborating hint, user-confirmed; custom groupings beyond the domains-only root table), surfaces a `## Compatibility` findings block (omitted when none), and on approval orchestrates in strict order: relocate tombstones → `_shared/removed-domains.md` (the one mechanical file reorg authors) → dispatch `/docs-hydrate-memory` backfill mode as a general-purpose sub-agent (no manifest, defer-regen) → rebalance + single `fab memory-index`. Restructure/author seam preserved (reorg relocates mechanically; per-file synthesis lives in hydrate's backfill). Decline = report + stop without mutating. (5ewp)

## 2026-06-13
- **Update** [templates](/memory-docs/templates.md) — refactor: Extract _intake shared helper for pre-boundary intake creation (#412)
- **Update** [templates](/memory-docs/templates.md) — **Skill Frontmatter gains the `helpers:` decomposition note** (Change B of the skill-architecture refactor trio): added a `#### helpers:` and the One-Shared-Helper-Per-Phase Decomposition subsection recording the **symmetry table** (`_generation` artifact mechanics / `_review` review mechanics / `_intake` pre-intake orchestration / `_pipeline` post-intake orchestration), completed by 3xaj when `_intake` (the pre-boundary counterpart to the post-boundary `_pipeline`) was extracted. Notes the internal-helper frontmatter shape, the flat canonical-source-vs-deployed-`SKILL.md` layout distinction, and the consumer-declared (no helper-side `helpers:`) model. The allowlist + per-skill mapping stay authoritative in [_shared/context-loading.md](/_shared/context-loading.md) (cross-referenced, not duplicated). `description:` frontmatter updated to surface the decomposition. (3xaj)

## 2026-06-12
- **Update** [hydrate](/memory-docs/hydrate.md) — docs: Docs Reality Sweep (#405)
- **Update** [hydrate](/memory-docs/hydrate.md) — fix: Operator/Pane/Tmux Surface — Perf + Error Surfacing (Binary-Review B5) (#398)
- **Update** [hydrate](/memory-docs/hydrate.md) — **Index Ownership model stated once** (skills-audit batch 5/5, Theme 8): the skill file gained an `### Index Ownership` section — indexes at all three tiers are generated artifacts (`fab memory-index` single writer); `description:` frontmatter is the **single hand-curated field**; a new domain/sub-domain `index.md` **stub** (description-only) is created **BEFORE** `fab memory-index` runs, which fills the body and round-trips the description. Both modes follow it; propagated to `docs-reorg-memory` Step 5.3/5.4 (stub-before-index resolves the edit-vs-forbid contradiction) and `/fab-continue`'s hydrate step. Ingest Step 3.2 now instructs the stub (incl. sub-domain stubs); index-regeneration tier wording names root + domain + **sub-domain** in both modes. The skill file also gained an explicit `## Context Loading` override section (skips the always-load layer — the override the `_preamble.md` §1 derivation rule keys on). (d9rs)
- **Update** [hydrate](/memory-docs/hydrate.md) — Index Maintenance "Last Updated" date-sourcing corrected to the shipped mechanism (binary-review B5, F34): `fab memory-index` now sources dates from ONE batched `git -c core.quotepath=off log --date=short --format=%x00%ad --name-only -- docs/memory` pass (newest-first, first date per path wins, keyed relative to the git top-level) instead of one `git log -1` spawn per memory file; the per-file call is kept solely as fallback when the batched call fails (a per-path miss = uncommitted → `—`, as before). Rendered index output is byte-identical. Mechanism description only — no skill-behavior change. (pw3k)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — docs: Docs Reality Sweep (#405)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — **Generate mode gains explicit placement rules** (skills-audit batch 5/5, Theme 8): target path (`{domain}/{topic}.md` or sub-domained), domain-folder creation with a `description:`-only index stub created before index regen (sub-domain stub likewise — the stub-before-index pattern of the Index Ownership model in [hydrate](/memory-docs/hydrate.md)), and the same shape bounds as ingest (~5–12/folder, depth ≤3, ≥8-file cluster for a sub-domain, `_shared`/`_unsorted` exempt). Previously these rules existed only under ingest mode. Index Maintenance step 2 now names the sub-domain index tier. (d9rs)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — docs: Docs Reality Sweep (#405)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — **No-target branch added** (skills-audit batch 5/5, Theme 8): when no existing spec file suits a gap, Step 5 proposes a new `docs/specs/{kebab-topic}.md` (full-content preview, sibling tone); on `yes` the file is created and its row added to `docs/specs/index.md` — still gated per-gap, specs stay human-curated. **Prompt/handler tokens aligned**: handler accepts exactly `yes`/`no`/`done` (the tokens Step 5 offers), `skip rest` demoted to a `done` alias (previously handled but never offered). (d9rs)
- **Update** [specs-index](/memory-docs/specs-index.md) — docs: Docs Reality Sweep (#405)
- **Update** [specs-index](/memory-docs/specs-index.md) — fix: Skills Staleness Sweep + Frontmatter Description Accuracy (Skills-Review Batch 2/4) (#391)
- **Update** [specs-index](/memory-docs/specs-index.md) — **Reserved-path exemption** (skills-audit batch 5/5): `docs-reorg-specs` never proposes renaming/moving/merging/splitting the constitution-pinned `docs/specs/skills/SPEC-*.md` mirrors (read-only for theme analysis; reserved-path Migration Map rows invalid) and its Step 1 now recurses into `docs/specs/` subfolders. New "SPEC Mirrors Are Reserved Paths in Spec Reorganization" design decision. **Mirror resync recorded**: ~23 drifted mirrors resynced against post-batch sources (Theme 7c) and `SPEC-hooks.md` rewritten as-shipped (Go `fab hook` system; no deleted shell scripts, UserPromptSubmit registered). (d9rs)
- **Update** [templates](/memory-docs/templates.md) — docs: Docs Reality Sweep (#405)
- **Update** [templates](/memory-docs/templates.md) — fix: Operator/Pane/Tmux Surface — Perf + Error Surfacing (Binary-Review B5) (#398)
- **Update** [templates](/memory-docs/templates.md) — fix: Skills Staleness Sweep + Frontmatter Description Accuracy (Skills-Review Batch 2/4) (#391)
- **Update** [templates](/memory-docs/templates.md) — **Depth bound reconciled** (skills-audit batch 5/5, Theme 8): the Memory Tree Shape depth bullet now states both countings as equivalent — ≤3 *path segments* under `docs/memory/` ≡ *folder depth* ≤2 (domain = 1, sub-domain = 2) — matching `docs-reorg-memory`'s reconciled Shape Report (`⚠ over depth` fires for folders deeper than 2). **Rebalancer apply path updated**: each new sub-domain's `description:`-only index stub is created BEFORE `fab memory-index` runs (Index Ownership model — resolves the former Step 5.3/5.4 edit-vs-forbid contradiction), and the no-dangling-link hard block gained an abort/rollback escape (unrewritable link → roll back that migration, re-run `fab memory-index`, continue with the rest). Hydration Rule 4 names all three index tiers. (d9rs)
- **Update** [templates](/memory-docs/templates.md) — Index Hierarchy date-sourcing description corrected (binary-review B5, F34): `fab memory-index` dates come from ONE batched `git log --date=short --name-only -- docs/memory` pass (newest-first, first date per path wins), with the per-file `git log -1` spawn retained solely as fallback when the batched call fails — replacing the stale "`git log -1 --date=short` dates" wording. Rendered output byte-identical; mechanism description only. (pw3k)

## 2026-06-11
- **Update** [specs-index](/memory-docs/specs-index.md) — Added the "Per-Skill SPEC Mirrors" section (H): mechanical `SPEC-{source-filename}.md` naming with partials keeping the leading underscore (`SPEC-preamble.md` renamed to `SPEC-_preamble.md`; live ref in `_shared/context-loading.md` updated, historical changelog rows exempt); four new SPECs created (`internal-consistency-check`, `internal-retrospect`, `internal-skill-optimize`, `_generation`); explicit exclusion policy for the pure-reference partials `_cli-fab`/`_cli-external` (CLI mirrors, not behavior — a SPEC would be a third copy), documented in `docs/specs/skills.md` § New Skill Checklist together with the naming convention. (uliv)
- **Update** [templates](/memory-docs/templates.md) — **Acceptance R# exemption (D, behavior-flagged)**: the R# trace mandate is scoped to requirement-derived categories — Code Quality and `checklist.extra_categories` items use `A-{NNN}: {outcome}` with no R# (matching the template's own A-007/A-008 shape); `_generation.md` steps 4+6 and the plan template's TRACEABILITY/ACCEPTANCE-FORMAT comments state the split. New design decision "Acceptance R# Mandate Scoped to Requirement-Derived Categories". **Template comment rewrites (C)**: the intake template's four spec-stage comments rewritten to plan/apply-entry vocabulary ("primary input for plan generation (apply entry)", "apply-entry agent (which co-generates plan.md)"); the plan template's `## Assumptions` comment now says **three grades only** (Unresolved is intake-only; `_preamble.md`'s four-grades rule scoped to intake artifacts — the intake template keeps all four). **Dead `**Status**:` lines deleted**: `**Status**: Draft` (intake) and `**Status**: In Progress` (plan) removed — no skill read or flipped them; `.status.yaml` is the state of record. `docs/specs/templates.md` mirror updated in the same change. (uliv)

## 2026-06-08
- **Update** [hydrate](/memory-docs/hydrate.md) — docs(memory): replace fab-workflow pseudo-domain with real topic domains (#381)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — docs(memory): replace fab-workflow pseudo-domain with real topic domains (#381)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — docs(memory): replace fab-workflow pseudo-domain with real topic domains (#381)
- **Update** [specs-index](/memory-docs/specs-index.md) — docs(memory): replace fab-workflow pseudo-domain with real topic domains (#381)
- **Update** [templates](/memory-docs/templates.md) — docs(memory): replace fab-workflow pseudo-domain with real topic domains (#381)

## 2026-06-07
- **Update** [hydrate](/memory-docs/hydrate.md) — Index Maintenance rewired to a mechanical `fab memory-index` call — the skill no longer hand-edits index rows. Ingest-mode behavior bullets updated (author `description:` frontmatter on files; run `fab memory-index` once at end). Index Maintenance requirement: root index is now **domains-only** (`| Domain | Description |`; the inlined per-file "Memory Files" / `file-list` column is dropped), domain rows are `| File | Description | Last Updated |` with descriptions from each file's `description:` frontmatter and git-stamped "Last Updated". Renamed the "Index Maintenance Embedded in Skill Instructions" design decision → "Memory Index Maintenance is a Mechanical `fab memory-index` Call" (superseded — the hand-maintained rows were the merge-conflict + drift source). (tciy)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — Index Maintenance rewired: generate mode now runs the mechanical `fab memory-index` (root = domains-only; domain rows = `| File | Description | Last Updated |`) once after generation instead of hand-updating `docs/memory/index.md` "with new domains and file lists". Structured Memory Output now requires a leading `description:` frontmatter line on each generated file (consumed by the generated domain index). (tciy)
- **Update** [templates](/memory-docs/templates.md) — Index Hierarchy gains a **sub-domain tier**: `fab memory-index` now generates `docs/memory/{domain}/{sub-domain}/index.md` (same `| File | Description | Last Updated |` file-row contract) for every sub-domain dir with ≥1 non-index `.md`, and the parent domain index gains a `## Sub-Domains` table emitted **only when sub-domains exist** (sub-domain-free domains render byte-identical to pre-`sx7a`). Affected Memory format documents the optional External `{domain}/{sub-domain}/{file-name}` addressing form alongside the flat form. Updated the Memory Tree Shape note: the `docs-reorg-memory` file-moving rebalancer apply path is now **complete** (move → both-direction link rewrite → frontmatter → `fab memory-index` → no-dangling-link guard), no longer a deferred `sx7a` follow-up. (sx7a)
- **Update** [templates](/memory-docs/templates.md) — Memory File Format gains a leading `description:` frontmatter field (curated one-liner, load-bearing for the generated index). Index Hierarchy reworked: both levels are now **generated by `fab memory-index`** (never hand-edited) — root index flipped to **domains-only** (`| Domain | Description |`; the inlined per-file "Memory Files" / file-list column dropped), domain rows are `| File | Description | Last Updated |` with descriptions from frontmatter and git-stamped dates. Hydration Rule 4 ("Update 'Last Updated' column / add entries for new files") replaced with "Run `fab memory-index`"; Rules 1–2 now author/maintain the `description:` frontmatter. Added a **Memory Tree Shape (SHOULD guidance)** subsection (~12 upper / ~5 floor / depth ≤3 / ≥8-file cluster to earn a sub-domain; `_shared`/`_unsorted` exempt; file-moving rebalance deferred to `docs-reorg-memory` follow-up `sx7a`). New design decision "Generated Memory Index + `description:` Frontmatter (Domains-Only Root)". (tciy)

## 2026-06-01
- **Update** [templates](/memory-docs/templates.md) — Removed the `spec.md` template (its requirement discipline now lives as a `## Requirements` section in `plan.md`). Overview now lists two artifact templates (intake, plan). Replaced the `### spec.md` section with a `## Requirements` description inside the `plan.md` section (RFC-2119 + GIVEN/WHEN/THEN, stable `R#` IDs, optional Non-Goals/Design-Decisions/Deprecated-Requirements subsections, NO `[NEEDS CLARIFICATION]`). `plan.md` parser contract is now three `##` parent headings (`## Requirements` apply-entry + review, `## Tasks` apply, `## Acceptance` review). **Trace annotations made REQUIRED** (`<!-- R# -->` on tasks, `R#` on acceptance). `### Removal Verification` distinction repointed from `spec.md` to `plan.md` `## Requirements > ### Deprecated Requirements`. Generation note reads `intake.md` (not `spec.md`). `.status.yaml` template progress map → 6 stages (dropped `spec: pending`); confidence-block note: `fab score` reads `intake.md`, `indicative` flag retired (no `indicative` key). Renamed/updated design decisions: "Single spec.md Without Delta Markers" → "Requirements Without Delta Markers", "Acceptance Validates Implementation, Not Requirement Quality", "Non-Goals Before Requirements", "Optional Sections as Guidance Comment", "SRAD-Driven Open Questions". (j6cs)

## 2026-05-07
- **Update** [hydrate](/memory-docs/hydrate.md) — Added Overview disambiguation: this file documents the standalone `/docs-hydrate-memory` skill; the `/fab-continue` pipeline hydrate stage (now reads `## Deletion Candidates` from `plan.md` informationally as Step 3) is documented in [execution-skills](/pipeline/execution-skills.md). No changes to `/docs-hydrate-memory` behavior. (ogf2)
- **Update** [templates](/memory-docs/templates.md) — `plan.md` parser contract extended from two to three heading-keyed sections: alongside `## Tasks` (apply parses) and `## Acceptance` (review parses), there is now an OPTIONAL `## Deletion Candidates` section appended by review (`_review.md` Validation Step 8) when findings exist or — for non-skip change types — the literal "None — ..." line. The section is review-owned and review-generated; apply does not write it; the template scaffold MUST NOT include a placeholder. Hydrate MAY read it informationally. Distinct from `## Acceptance > ### Removal Verification` (planned vs discovered removals). Omitted entirely for `[docs, chore, ci]` change types. (ogf2)

## 2026-05-06
- **Update** [hydrate](/memory-docs/hydrate.md) — Reviewed for `tasks.md`/`checklist.md` references in light of the apply-stage artifact merge into `plan.md`. No live references found — this file documents the standalone `/docs-hydrate-memory` skill (URL/folder ingest + generate from codebase), not the `/fab-continue` pipeline-stage hydrate behavior, and it never named those legacy artifacts. No changes required. (qszh)
- **Update** [templates](/memory-docs/templates.md) — Replaced `tasks.md` + `checklist.md` template entries with a single `plan.md` entry (`## Tasks` + `## Acceptance` heading-keyed sections; stable parser contract). Updated `.status.yaml` template to 7-stage progress map (dropped `progress.tasks`) and replaced `checklist:` block with `plan:` block (`generated`, `task_count`, `acceptance_count`, `acceptance_completed`; dropped `path`). New plans use `A-NNN` acceptance IDs; in-flight migrations preserve `CHK-NNN` verbatim. Renamed "Checklist Validates Implementation" → "Acceptance Validates Implementation" and "Checklist Co-located at Change Root" → "Plan Co-located at Change Root". Added two design decisions: `plan.md` replaces tasks+checklist (single apply-stage artifact) and `A-NNN` for new plans; `CHK-NNN` preserved for in-flight migrations. (qszh)

## 2026-02-27
- **Update** [templates](/memory-docs/templates.md) — Renamed `issue_id: null` → `issues: []` (scalar to array), `shipped: []` → `prs: []`. All access via statusman `add-issue`/`get-issues`/`add-pr`/`get-prs`. (gasp)
- **Update** [templates](/memory-docs/templates.md) — Template `intake: active` → `intake: pending`. `changeman.sh start` transitions to `active` with metrics. `/fab-new` advances to `ready`. Clean layer separation: template (blank slate) → script (activate) → skill (generate + advance). (ijql)

## 2026-02-26
- **Update** [templates](/memory-docs/templates.md) — Added `issue_id: null` to .status.yaml template initial state. (jq7a)
- **Update** [templates](/memory-docs/templates.md) — Changed `.status.yaml` template default `change_type` from `feature` to `feat`, aligning with the 7-type conventional commit prefix taxonomy. (tnr8)

## 2026-02-18
- **Update** [hydrate](/memory-docs/hydrate.md) — Replaced stale `/fab-init` → `/fab-setup` (3 occurrences) and `lib/sync-workspace.sh` → `sync/2-sync-workspace.sh` in glob pattern reference (5isu)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — Replaced stale `/fab-init` → `/fab-setup` in pre-flight check guidance (5isu)
- **Update** [specs-index](/memory-docs/specs-index.md) — Replaced stale `/fab-init` → `/fab-setup` in bootstrap integration reference (5isu)
- **Update** [templates](/memory-docs/templates.md) — Replaced stale `lib/sync-workspace.sh` → `sync/2-sync-workspace.sh` in model tier deployment reference (5isu)

## 2026-02-15
- **Update** [templates](/memory-docs/templates.md) — Added Code Quality to checklist categories (always included, two baseline items). Updated generation derivation sources to include `code_quality` config (r8k3)
- **Update** [templates](/memory-docs/templates.md) — Renamed template `brief.md` → `intake.md`. Strengthened template comments (What Changes, Origin, Why sections). Updated all artifact references from brief to intake (v4n7)

## 2026-02-14
- **Update** [hydrate](/memory-docs/hydrate.md) — Updated hydration target paths from `fab/memory/` to `docs/memory/` (m3v8)
- **Update** [hydrate](/memory-docs/hydrate.md) — Renamed `_init_scaffold.sh` → `lib/sync-workspace.sh` in glob pattern reference (q7f2)
- **Update** [hydrate](/memory-docs/hydrate.md) — Renamed skill from `/fab-hydrate` to `/docs-hydrate-memory`; updated skill file path, glob pattern reference, and all cross-references (r8kv)
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — Updated path references from `fab/memory/` and `fab/specs/` to `docs/memory/` and `docs/specs/` (m3v8)
- **Update** [specs-index](/memory-docs/specs-index.md) — Updated all path references from `fab/specs/` to `docs/specs/` (m3v8)
- **Update** [templates](/memory-docs/templates.md) — Added confidence block initial state note: score 0.0 (no assessed confidence). Updated template default from 5.0 to 0.0. (lptw)
- **Update** [templates](/memory-docs/templates.md) — Renamed `_calc-score.sh` → `lib/calc-score.sh` and `_init_scaffold.sh` → `lib/sync-workspace.sh` in references (q7f2)

## 2026-02-13
- **Update** [templates](/memory-docs/templates.md) — Updated `.status.yaml` template example (`archive: pending` → `hydrate: pending`), hydration rule references, and checklist gate references to use `hydrate` instead of `archive` (jc0u)
- **Update** [templates](/memory-docs/templates.md) — Flattened intake Affected Memory to inline markers, removed BLOCKING/DEFERRED from Open Questions, replaced spec optional section placeholders with guidance comment (v4rx)

## 2026-02-12
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — Renamed from `/fab-backfill` to `/docs-hydrate-specs` for semantic consistency with `/fab-hydrate` (akhp)
- **Update** [templates](/memory-docs/templates.md) — Moved checklist from `checklists/quality.md` to `checklist.md` at change root; updated .status.yaml template `checklist.path` default (ipoe)
- **Update** [templates](/memory-docs/templates.md) — Added optional Non-Goals and Design Decisions sections to spec.md template (r4w8)
- **Update** [templates](/memory-docs/templates.md) — Updated .status.yaml template (removed stage: field and intake: from progress, spec: active as initial), documented intake template Origin section (v5p2)

## 2026-02-11
- **Update** [specs-index](/memory-docs/specs-index.md) — Consistent design terminology, updated from specs-index references (r3k8)
- **Update** [templates](/memory-docs/templates.md) — Added `created_by` field to `.status.yaml` template — auto-detected from `git config user.name`, write-once, with `"unknown"` fallback (endg)
- **Update** [templates](/memory-docs/templates.md) — Renamed proposal.md → intake.md, removed plan.md template section (r3k8)

## 2026-02-09
- **Update** [hydrate-specs](/memory-docs/hydrate-specs.md) — Initial creation — `/docs-hydrate-specs` skill for detecting and hydrating structural gaps from memory to specs (h3v7)
- **Update** [specs-index](/memory-docs/specs-index.md) — Updated Human-Curated Ownership section to reference `/docs-hydrate-specs` as assisted reverse-hydration (h3v7)

## 2026-02-08
- **Update** [hydrate](/memory-docs/hydrate.md) — Fixed stale `doc/fab-spec/TEMPLATES.md` reference in Index Maintenance to `docs/specs/templates.md` (4wg3)

## 2026-02-07
- **Update** [hydrate](/memory-docs/hydrate.md) — Added generate mode — unified argument routing, dual-mode overview, cross-reference to hydrate-generate doc (k5od)
- **Update** [hydrate](/memory-docs/hydrate.md) — Created hydrate doc — extracted `/docs-hydrate-memory` as standalone skill from `/fab-init` Phase 2 (q7m3)
- **Update** [hydrate](/memory-docs/hydrate.md) — Fixed command references from `/fab-xxx` colon format to `/fab-xxx` hyphen format (sawf)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — Created — generate mode requirements and design decisions for `/docs-hydrate-memory` (k5od)
- **Update** [hydrate-generate](/memory-docs/hydrate-generate.md) — Fixed command references from `/fab-xxx` colon format to `/fab-xxx` hyphen format (sawf)
- **Update** [specs-index](/memory-docs/specs-index.md) — Initial creation — added `docs/specs/` directory, design index with boilerplate, bootstrap and context loading integration (bb1q)
- **Update** [templates](/memory-docs/templates.md) — Fixed command references from `/fab-xxx` colon format to `/fab-xxx` hyphen format (sawf)
