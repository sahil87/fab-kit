---
description: "Cross-cutting concerns spanning all domains — config.yaml/constitution/5 Cs and the context-loading convention (always-load layer, SRAD, selective loading)"
---
# Shared Documentation

> **Generated by `fab docs-index`** — everything outside the manual block below is regenerated from the tree and each file's own metadata (`description:` frontmatter, HTML `<title>`/`<meta name="description">`): don't edit it, re-run the command. The manual block is the one hand-managed region: edit it, it is preserved verbatim.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed; preserved verbatim on regeneration. Add rows the generator
     cannot produce: non-markdown files' descriptions (HTML, PDF), external links,
     custom groupings. A row for a file the walker also indexes keeps its place and
     label here, but its description follows that file's frontmatter. A row
     whose target no longer exists is flagged by `fab docs-index --check`. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [configuration](configuration.md) | `config.yaml` schema + the 5 Cs of Quality. Four-tier env > system > project > built-in resolution — per-leaf deep merge, empty leaves fall through — with scope-gated `FAB_*` overrides; provider capability grammar; the `agent.session`/`agent.workers` depth knobs; the six-verb `fab config` surface — surgical writes and the short scope-annotated fence; `dispatch.{mode,column_width,min_cols,min_rows,reap_done}`, `autopilot.merge_mode`, the pane → native → headless descent. |
| [context-loading](context-loading.md) | Smart context loading: the seven-file baseline, opt-in helpers, standard subagent context, domain lookup, SRAD and Next conventions. Per-stage YAML resolution maps stage → role → provider/profile, applies the shared dispatch.mode descent ladder, and preserves the `dispatch:` key-presence seam with bounded CLI-worker recovery. |
