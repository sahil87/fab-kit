---
description: "Cross-cutting concerns spanning all domains — config.yaml/constitution/5 Cs and the context-loading convention (always-load layer, SRAD, selective loading)"
---
# Shared Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [configuration](configuration.md) | `config.yaml` schema + the 5 Cs of Quality. Four-layer env > project > system > built-in resolution with scope-gated `FAB_*` overrides; provider capability grammar (`session_command`, `dispatch_command`, `native`); the `agent.session`/`agent.workers` depth knobs; the six-verb `fab config` surface (show/explain/set/unset/init/upgrade) with surgical writes and managed fence; `dispatch.{mode,column_width,reap_done}` with the pane → native → headless descent. |
| [context-loading](context-loading.md) | Smart context loading: the seven-file baseline, opt-in helpers, standard subagent context, domain lookup, SRAD and Next conventions. Per-stage resolution maps stage → role → provider/profile, applies the shared dispatch.mode descent ladder, and preserves the presence-based `dispatch=` seam with bounded CLI-worker recovery. |
