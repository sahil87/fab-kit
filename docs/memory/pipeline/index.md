---
description: "The change pipeline — stage lifecycle & state machine, planning/clarify/execution skills, schemas, preflight (intake → apply → review → hydrate → ship → review-pr)"
---
# Pipeline Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [change-lifecycle](change-lifecycle.md) | Change naming, folder structure, `.status.yaml` (6-stage pipeline, `plan:` block, flock-serialized fsynced writes, sparse-key insertion), the `.fab-status.yaml` active-pointer symlink (atomic swap, dangling-target validation), `stage_hooks`, git integration incl. the ship-time branch↔change guard, `/fab-status`, `/fab-switch`, `/git-branch`, backlog scanning, the `summary` log field + generated FKF artifacts (per-folder `log.md`), and `/fab-adopt` adoption via the honest `apply: skipped` state. |
| [clarify](clarify.md) | `/fab-clarify` skill — intake-only, dual modes (suggest/auto), intake taxonomy scan, structured questions, coverage reports, audit trail, grade reclassification, always-recompute intake score; hosts the [AUTO-MODE] Skill Invocation Protocol and is the sole bulk-confirm authority; bulk confirm evaluated before the zero-gaps exit, confirmations graded by recomputed composite (no fiat Certain), unified audit-trail placement rules |
| [execution-skills](execution-skills.md) | Pipeline execution-skill behavior — /fab-continue (apply generates a unified plan.md; review is a SINGLE inline agent; hydrate rewrites each topic section to current truth under the writer contract — no change-id headings, rationale in four-field Design Decisions, post-body-edit description re-check + post-hydrate self-check, never hand-merges generated indexes), the shared /fab-ff and /fab-fff bracket in _pipeline.md, /git-pr ship, /git-pr-review, /fab-adopt, /fab-archive. |
| [hooks-may-enhance-never-own](hooks-may-enhance-never-own.md) | The governing principle 'hooks may enhance, never own' + the pull-based artifact-state model: correctness-critical `.status.yaml` state — change_type, confidence, plan counts — is recomputed by `fab status refresh` and self-healed at the forward+orient seams (`fab status advance`/`finish`, `fab preflight`), never owned by a harness hook; why start/reset/skip/fail are excluded; there are zero fab hooks — a hook is justified only for genuinely underivable, harness-local state. |
| [planning-skills](planning-skills.md) | `/fab-new`, `/fab-draft`, `/fab-clarify`, `/fab-ff`, `/fab-fff` — the intake-only planning stage: the shared `_generation` (Intake + unified Plan, plus Intake-from-Diff/Plan-from-Diff for `/fab-adopt`) and `_intake` (Steps 0–9, interactive | promptless-defer) helpers with thin call-sites; re-run/ID-collision resume semantics; pull-based change_type with sticky explicit set; SRAD v2 demerit confidence scoring in the `_srad` helper; fab-ff/fab-fff as thin wrappers over the `_pipeline` bracket. |
| [preflight](preflight.md) | `lib/preflight.sh` script — validation, accessor-based architecture, structured YAML output, skill integration |
| [schemas](schemas.md) | Workflow schema authority — the Go state machine (`internal/status` + `internal/statusfile`): 6-stage pipeline, states, transitions, validation, 216-cell matrix test; the `.status.yaml` block schemas (`plan:`/`confidence:`/`true_impact:`/`summary:`/`change_type_source:`); `fab score`/`impact`/`pr-meta` + the `fab status` query `--json` surface; `log.md` C-lite + seed-merge + freeze-on-write; the malformed-frontmatter-blocking vs advisory description-length `fab memory-index --check` asymmetry. |
