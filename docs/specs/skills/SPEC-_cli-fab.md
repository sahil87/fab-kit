# _cli-fab
Fab CLI command reference — the exhaustive companion to the Common fab Commands table in `_preamble.md`: full flag/argument surface, calling convention, extended `fab score` formula and `.status.yaml` schema, common error messages. A lookup catalog, not a procedure; loaded selectively via `helpers: [_cli-fab]`.
### Tools used
None — a reference document consumed by skills (looked up, not executed).
### Sub-agents
None.
