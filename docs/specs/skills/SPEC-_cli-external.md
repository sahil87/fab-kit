# _cli-external
External CLI tool reference — the non-fab tools used for multi-agent coordination (wt, idea, hop, tmux, rk, /loop), carrying only fab-owned integration notes; tool-owned usage is delegated at use-time to `<tool> skill` / `<tool> help-dump`. Internal partial loaded by operator skills only.
### Tools used
None — a reference document consumed by operator skills (looked up, not executed).
### Sub-agents
None.
